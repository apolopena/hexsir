# TASKS.md — Running Ledger

> **Task Codes:** MAJOR-X for cross-cutting work spanning multiple subsystems (library + CLI + docs + tooling in one pass) | POST-IMPL-X for substantial implementations | MAINT-X for maintenance (fixes, tweaks, refactoring)
>
> **Ordering:** Newest entries at top.
>
> **Scope:** Describe work, not results. Keep test/lint pass counts and coverage totals out. Mention tests only when the work is authoring or refactoring them.

---

## In Progress
<!-- IN_PROGRESS_START -->
*(empty)*
<!-- IN_PROGRESS_END -->



## Backlog

- **CLI shape: `tier` subcommand is ambiguously scoped.** `rerw write savefile tier --slot N --tier T` is currently a top-level field, but tier applies to BOTH talents and items. Needs scoping under the relevant noun (e.g. `rerw write savefile talent tier --slot N --tier T` and a parallel `item tier`), or the existing `tier` subcommand needs explicit talent/item disambiguation. Holding off until the runtime tier-roller (Frida path B) is solved, since findings there may inform what other tier-related save edits we want to expose.



## Done
<!-- DONE_START -->
MAINT-44: Hourglass power — reward-item spawn-on-demand wrapper (2026-05-07)
  - New `tools/frida/mods/powers/Hourglass.js` (v0.3.0). Thin wrapper around the verified `SpawnerProbe.expr_summonAtPlayer({nameMatch:"NoModel+2Cpnt", index:0, noWarp:true})` primitive — fires the chapter hourglass's early-boss-reward item on demand. Auto-loads + arms `spawner_probe` on power load (`RW.loadMod("spawner_probe")` then `expr_armSpawnerCtor()` if not already armed); user only calls `loadPower("Hourglass")`.
  - API: `spawnItem()` (one-shot, quiet), `spawnItem({intervalMs})` (periodic, quiet), `spawnItem({verbose:true})` (let underlying logs through), `spawnItemStop()`. Quiet by default — wraps the underlying call in a `console.log` redirect to keep interval mode from flooding the REPL. Pre-active hourglass: one fire per state transition; active hourglass (player out of safe zone): re-fires indefinitely.
  - Registered in `tools/frida/rw_lab.js` header comment block + startup log line. New `*` marker convention added — flags powers that must be loaded before chapter (or game) start because their hook arms during setup. Hourglass is the first; future powers hooking once-per-chapter ctors get the same mark.
  - Cross-references `rw/findings/entity-spawner-mechanism.md` §"Live work — 2026-05-07 follow-up" for the underlying primitive's rationale and ruled-out alternatives.

MAINT-43: warden — drop file logging, stdout only (2026-05-07)
  - Removed `blocked.log` writes from `warden.py`; stdout is the only output channel now. Renamed internal `log_block` → `report_block` to match.
  - Deleted `tools/telemetry-warden/.gitignore` (no longer needed since no log file is generated).
  - Updated `tools/telemetry-warden/README.md` to drop log-file references and note that users wanting persistence can redirect stdout themselves.

MAJOR-12: Steam-vs-analytics RE + telemetry-warden block tool (2026-05-07)
  - **Steam playtime question answered.** Investigated user-reported "save's logged playtime gets added to Steam profile playtime on each test." Confirmed the binary does not (and cannot via Steamworks public API) write to Steam profile playtime. The three "playtime"-named strings in the binary are: `playtime.game` and `playtime.run` (debug-overlay scratch keys, written to internal 64-slot in-process key/value table at `debug_overlay_set_kv`), and `run_playtime` (JSON field in studio analytics POST). None reach Steam. Most likely cause of observation: Steam profile's 0.1h (6-min) rounding granularity compounded over multiple short tests.
  - **Telemetry surface fully mapped.** `passtech_analytics_post_event` (formerly `FUN_1401f6b10`) is the master JSON POST builder — 33 distinct callers, 37 callsites, every game-state event funnels through here. Endpoints: `dt-live{,-2,-3}.passtechgames.com` (Pub/Sub topic `projects/passtech-ravenswatch/topics/ravenswatch`), `nacon-os-rec{,-v2}-54f75zaw5q-ew.a.run.app` (Cloud Run), `nacon-os.com/v2/analytics`, plus `submit.backtrace.io` for crash reports. Auth via `os-access-token` HTTP header. JSON includes user_id, session_id, hero_name, hero_skin, difficulty, run_playtime, run_id, nacon_user_id.
  - **Steamworks usage is narrow and delay-loaded.** No static IAT thunks for `steam_api64.dll`. Three interfaces queried by name: ISteamUser (`SteamUser023`), ISteamUserStats (`STEAMUSERSTATS_INTERFACE_VERSION012`, used by `oCSteamAchievementsManager` for achievements), ISteamApps (`STEAMAPPS_INTERFACE_VERSION008`). No SteamRemoteStorage (confirms Steam Cloud is disabled). Accessors follow the SDK `STEAM_DEFINE_USER_INTERFACE_ACCESSOR` pattern.
  - **`tools/telemetry-warden/` block tool.** Standalone Python (~130 lines, stdlib only). Binds `127.0.0.1:80/443/8888`, peeks TLS ClientHello SNI or HTTP Host header, logs `BLOCKED <host>:<port>` to `blocked.log`, drops the connection. Combined with eight hosts-file entries, every telemetry call is intercepted at localhost — zero bytes leave the machine. End-of-session per-host count summary. Deliberately scoped narrow per "do one thing" feedback: no TLS termination, no payload inspection, no rules engine, no install scripts. README documents Windows-side invocation via UNC path.
  - **Hosts file entries** (manual one-time edit by user): `dt-live{,-2,-3}.passtechgames.com`, `dt-dev.passtechgames.com`, `nacon-os.com`, `nacon-os-rec{,-v2}-54f75zaw5q-ew.a.run.app`, `submit.backtrace.io` → `127.0.0.1`. Wrapped in `# === BEGIN/END ravensmith telemetry-warden ===` markers for trivial reversal.
  - **Ghidra annotations:** 5 function renames (`debug_overlay_set_kv`, `passtech_analytics_post_event`, `SteamUserManager_get_ISteamUserStats`, `SteamUserManager_get_ISteamUser`, `Steam_init_and_register_lobby_handlers`) plus 5 plate comments documenting slot layout, JSON shape, endpoint list, and SDK accessor pattern.
  - Two new findings: `rw/findings/steam-playtime-not-game-driven.md` and `rw/findings/telemetry-surface-and-warden.md`. STATUS.md regenerated (46 entries).
  - Also added `.claude/commands/rw-kickoff-parallel.md` (parallel-session command with read-only Ghidra default) and added `disable-model-invocation: true` to `rw-context-handoff.md`.

MAJOR-11: Cooked-format per-class schemas + `.ot` text decoder + spawner-settings intel (2026-05-07)
  - **Per-class schema framework** layered on top of existing `cooked.py` framing decoder. Four new schemas in `tools/rerw-src/lib/cooked_schemas.py`: `oCDtHeroDefinition` (schema_v=26, 30+ fields), `oCEntitySettings` (v=8, 6 fields), `oIEntityCpntSettings` (v=20, 12 fields, embedded as parent prefix), `oCEntityCpntEntitySpawnerSettings` (v=21, 14 own fields + parent prefix). Wired into `--annotate` CLI which auto-applies registered schemas to every framed object (top-level and nested).
  - **Nine reusable helper readers** (`two_string`, `composite`, `vec_obj_ref`, `vec_two_string`, `u8_array_fixed5`, `u8_array_fixed16`, `two_string_array_fixed5`, `named_subobject`, `vec_picker`) compose into any future class schema. Each helper's stream byte-cost reverse-engineered from its decompile.
  - **`oCBinaryLoader` vtable byte-offset map** decoded — `+0x60` string, `+0x70` u8, `+0x90`/`+0x98` u32, `+0xa0` named-sub-object, `+0xa8` ReadObjectRef. Reusable for every Cooked class's `Serialize()`. Vtable data label persisted in Ghidra at `0x140f23120`.
  - **Plain-text `.ot` manifest decoder** new at `tools/rerw-src/lib/ot_text.py` — pipe-delimited `<resource_type>|<path>|<class>` records with summary / full / class-filter modes.
  - **Round-trip validated** byte-for-byte on 13 hero `.gen` files (every harvested herodef), Geppetto's 620 KB entity-settings file, and the 3 KB Puppet variant. Schema-decode clean across all 16 files (no errors, no unparsed bytes).
  - **Parallel-session open question closed** — `entity-spawner-mechanism.md` §"Open questions / next steps" item #3 ("decompile `oCEntityCpntEntitySpawnerSettings` to find the tether-vs-independent flag and 'what to spawn' template field"). Empirical findings on Geppetto's 4 spawner instances: struct field at `+0x1bc` is a u32 enum with engine-validated bound `≤ 2` (matches the three-way pool-flavor taxonomy in `entity-spawner-mechanism.md` §"Pool-ownership taxonomy"); `+0x138` is a named sub-object whose body decodes as a `(resource_type, path)` two_string pair pointing at the spawn target's entity-settings file (the static-readable equivalent of the runtime spawner's `+0x18` settings binding). Both Geppetto spawners observed have tether=0; verifying enum semantics on a known-tethered case (e.g., Cultist Summoner's tentacle spawner) is the cited next-step probe.
  - **24 Ghidra renames persisted** — 7 class methods (`oCDtHeroDefinition_{typedesc_init,ctor,Serialize}`, `oCDtDefinition_Serialize`, `oCEntitySettings_Serialize`, `oCEntityCpntEntitySpawnerSettings_Serialize`, `oIEntityCpntSettings_Serialize`), 8 helper functions (`serde_two_string`, `serde_composite_resource_link`, `serde_vec_obj_ref`, `serde_vec_two_string`, `serde_u8_array_fixed5`, `serde_two_string_array_fixed5`, `serde_subobj_array_fixed7`, `serde_vec_picker`), 7 `oCBinaryLoader` vtable methods (`oCBinaryLoader_{read_u32,read_8bytes,read_u8,read_string,read_named_subobject,is_writing,get_class_schema_version}`), 2 vtable data labels (`oCDtHeroDefinition_vftable` at `0x140f04110`, `oCBinaryLoader_vftable` at `0x140f23120`).
  - **Findings layer.** New `rw/findings/cooked-format-schemas.md` (confirmed status) — full schema field tables, helper inventory with stream-cost formulas, loader vtable map, locating-symbols section for re-anchoring, tool-reference section, six worked example invocations.

MAJOR-10: Entity-spawner mechanism static dig + game-rules + AI-target findings (2026-05-07)
  - **Entity-spawner mechanism mapped.** `oCEntityCpntEntitySpawner` is the universal spawn component (engine-native term is "spawn", not "summon" — the `Summon` string only appears once in the binary). Vtable RVA `0xf00650`, class hash `0x33bd`, three Sc_* script-callables (Spawn `0x1faa`, Despawn `0x1fab`, IsSpawned `0x1fac`). Spawn primitive chain decompiled end-to-end: `Sc_Spawn_impl → spawnIfNotCached → spawnEntityFromBoundTransform → oCSpawnablePool_allocateNode → settings->vtable[10] (= FUN_1406c7a40 trampoline) → FUN_1406de800 → malloc(0x640) + oCEntity::ctor`. `oCEntitySettings` primary vtable confirmed at `0x140f4cee8` (vtable[10] = the universal entity factory trampoline) by ctor disassembly. `oCEntity` primary vtable fully decoded (15 entries at `0x140f4cc40`).
  - **Pool-ownership tether mechanism resolved.** Every `oCEntity` is allocated from an `oCSpawnablePool`; pool destruction destroys all alive-list entries in lockstep. Tethered (tentacle) vs independent (egg) is encoded by which pool the spawner targets via `+0x1c0` owner-backref — same component class for both flavors. Explains why prior session couldn't find a parent→children backref in entity bytes: the relationship lives in pool-allocation-state, not entity bytes.
  - **Death-listener mechanism resolved.** `+0x570` listener array on every `oCEntity` (count `+0x578`, capacity `+0x57c`). Spawners subscribe via thunk `LAB_1406feb40` → handler `0x1406ef380`; on child-death the handler clears spawner's `+0x160` cache and `+0x168` activation flag. **This is the "13 listeners per enemy / 393 events per cauldron fight" mechanism that `enemy-spawn-architecture.md` had dismissed** — every listener was a spawner subscription.
  - **15 Ghidra renames + 11 plate comments + 1 EOL comment** added per the "annotate findings on the spot" rule. Notable new symbols: `oCEntityCpntEntitySpawner_{ctor,dtor,registerScriptCallables,Sc_Spawn_impl}`, `oCEntitySpawner_{onAttachToParent,streamingGridRegister/Unregister,enableAndSpawn,batchSpawnTimeBudget,spawnIfNotCached,spawnEntityFromBoundTransform,setActiveFlag,recomputeActivationGate,despawnAndUnregister,clearAsyncIoVector}`, `oCSpawnablePool_{dtor,allocateNode}`, `oCEntitySettings_ctor`, `oCEntity_{getClassHash_stub,registerSettingsListener}`, plus a comprehensive vtable plate at `0x140f4cc40`.
  - **Two static-unresolved mysteries documented for WinDbg follow-up.** (a) `[*spawner + 0x178]` virtual call in `spawnEntityFromBoundTransform` — primary vtable has 30 entries (max offset `+0xe8`); offset `+0x178` reads past the end into RTTI strings + an unrelated class's vtable region; static decode produces a destructor address which is semantically nonsense as a "get bound transform" call. (b) `spawner->+0x108` (async I/O queue) — ctor leaves it as `-1` sentinel; real init happens externally (likely chapter-streaming orchestrator). Both blockers for constructing a from-scratch spawner that can fire spawn. Resolution path captured in finding doc: WinDbg breakpoint at `0x1406eefde` during a real cauldron spawn, inspect runtime values — 30-60 minutes when game running.
  - **Frida primitive that we CAN write with certainty:** `oCSpawnablePool_allocateNode(settings + 0x18)` produces a structurally-valid `oCEntity` shell — settings ptr at `+0x28`, empty component hashmap, linked into pool's alive-list. NOT a functional enemy (no components attached, no scene-context registration, no asset streaming). For full enemy spawn-from-arena, the recommended path is to hijack an existing in-world spawner (Sandman/hourglass/fireflies/teleporter in the starting arena have working I/O queues) by overriding its `+0x18` settings binding and clearing `+0x160`.
  - **Findings layer.** New in-progress `rw/findings/entity-spawner-mechanism.md` (613 lines) — TL;DR + Confirmed/Hypothesis/Tried-and-ruled-out + Locating-symbols + Re-anchoring recipe + Trigger-primitive section + Deeper-static-dig section. New in-progress `rw/findings/enemy-ai.md` (sparse seed) — pulls the `+0x08` Geppetto-Dummy AI-target observation out of `enemy-spawn-architecture.md` where it was buried under "engagement state" framing, frames it as the open "redirect AI to make enemies fight each other" question. New `rw/docs/game-rules.md` — gameplay-level rules doc separate from RE findings, first entry "Cauldrons and waves" encoding the test-design constraint that cauldron content is randomized per chapter run (no test can depend on a specific enemy family appearing). `CLAUDE.md` and `rw/docs/README.md` got pointers; `rw/docs/wishlist.md` got the AI-target dig entry. `rw/findings/enemy-spawn-architecture.md` TL;DR rewritten from "hybrid spawn architecture" to unified runtime-construction model (per the user's correction that all main enemies phase in via cauldron, not pre-placed-asleep).
  - **STATUS.md regenerated** via `findings-status` skill — 43 entries.

MAJOR-9: Enemy-spawn architecture + EnemyHerd live-control mod (2026-05-06)
  - **Enemy-spawn architecture identified as hybrid.** Two paths coexist: (a) chapter-load static placement (e.g., the 4 big spiders in a Spider Nightmare arena — woken by cauldron, NOT constructed by it), (b) runtime construction via the universal `oCEntity` factory chain (e.g., Cultist/Summoner cauldron — `Elite_Snake_Nightmare`, `Elite_Cultist_Summoner`, `Cultist_Summoner_Summoned_Tentacle` captured with full 6-component stacks during the fight). Effect entities (eggs, projectiles, puddles) always runtime. Earlier "all enemies pre-spawned" theory was incomplete.
  - **`oCDtEntityCpntEnemyController` class fully pinned.** RTTI string at `0x141353c30`, vtable RVA `0xf0a6c8`, ctor RVA `0x37bbe0`, classId getter RVA `0x37bbd0` (`mov eax, 0x1561073c; ret`), registerClass RVA `0x3be570`, dtor RVA `0x37bd00`, hash key `0x1561073c`. Three functions and one vtable renamed in Ghidra with plate comments documenting the registry pattern.
  - **Universal entity factory chain mapped statically.** Every `oCEntity` construction passes through `oCEntity::ctor` (RVA `0x6c96f0`) ← `FUN_1406de800` (allocator: `malloc(0x640) + ctor + virtual init at vtable[+0x08]`) ← `FUN_1406c7a40` (thin trampoline). The trampoline is reached *only* via function-pointer dispatch from the class-metadata table at `0x140f4cf38` — `+0x80` slot of a sub-struct reached via `+0x88` of the main metadata. Pattern is universal: every class registers its ctor pointer at this offset.
  - **Hooking `oCDtEntityCpntEnemyController_ctor` directly is a dead end.** Hook fires 0 times during gameplay — the engine doesn't go through that function pointer. `tools/frida/mods/enemy_capture.js` is **deprecated**; kept on disk as a record of the dead-end approach. The class-metadata-registered ctor exists for type-info / introspection, not as the runtime instantiation site.
  - **Stable `oCEntity` structural offsets discovered.** `+0x620` is a self-pointer (`*(entity + 0x620) == entity`) — verified on 2 captured enemies; useful as a memory-alive check. `+0x08` and `+0x10` hold *other* `oCEntity` pointers — engagement state (last damager, intrusive list-node `prev`/`next`), NOT parent-spawned-by backrefs (verified by scanning a captured Tentacle's first 1600 bytes for its known summoner address — summoner not present). Parent-traversal via entity bytes does not work; the relationship lives elsewhere (likely on the EnemyController component).
  - **`spawn_capture.js` v0.5.0 → v0.8.1.** Layered enhancements: v0.6.0 added `initArg`-name resolution (settings template `+0x08` char*, `+0x10` u32 length — mirrors encyclopedia walker pattern; `analyze()` now prints e.g. `Spider_Nightmare_Egg [0x...] (N entities)`). v0.7.0 dropped the position filter from `analyze()` (was culling dead enemies). v0.8.0 captures `this.returnAddress` per spawn and adds `findCommonParents({ templateSubstring, scanBytes })` (scans entity bytes for non-module qwords whose deref's vtable RVA is `0xf4cc40`). v0.8.1 captures a 4-frame `Thread.backtrace(... ACCURATE)`; `analyze()` prints frame[0]/frame[2]/frame[3] caller histograms. Frame[2] is the spawn-site target (frame[0] is uniform noise inside the allocator, frame[1] is the trampoline).
  - **`tools/frida/mods/cauldron_test.js`** — orchestration mod. Verifies a cauldron exists in the encyclopedia, refreshes the player capture, arms `SpawnCapture` (auto-loads), warps the cauldron via `Transporter` next to the player, prints walk-up-and-activate instructions; `finish()` clears the warp, stops capture, runs `analyze`. Removes 9-line REPL boilerplate and three procedural pitfalls (missing refresh, double-`start()` wiping captures, missing cauldron check).
  - **`tools/frida/mods/enemy_herd.js`** — live-enemy-control breakthrough. Reads `SpawnCapture._captures`, filters to entities with `EnemyController` component AND a valid `+0x620` self-pointer, every N seconds warps all but the anchor (`roster[0]`) to within `radius` units of the anchor via `vtable[+0x50] setPosition` (uniform-disc XZ scatter, anchor's Y preserved). Auto-stops when liveness filter empties. Verified working on Snakes and the Summoner; root-locked on Tentacles (engine likely re-asserts root transform every frame for that class).
  - **Findings layer.** New in-progress `rw/findings/enemy-spawn-architecture.md` covers all of the above plus a categorized template-name catalog (real-time enemies, spawn projectiles/effects, cauldron arena scaffolding, decorations, UI entities, anchors), a Locating section anchored on RTTI strings + hash-key byte patterns + the embedded class-name strings in the metadata table, and 4 open questions (parent-pointer location on the controller component, why Tentacles are root-locked, global per-instance entity registry, why `+0x80` ctor slot exists if not used at runtime). `rw/findings/transporter-placement-primitive.md` gained an Updates section with the spawn_capture changelog and cross-link to the new finding.
  - **Documentation overhaul: mermaid removed.** `rw/findings/save-flow-diagrams.md` rewritten from 11 mermaid blocks to prose + tables (no content change, format only — 11 sections preserved with all RVAs / class details / offsets intact). User decision: mermaid is NOT part of the workflow going forward; the locator-section convention (`rw/docs/README.md` §"Locating <thing>") is what carries the rebuild story for new binary versions. Two cross-references in `save-edit-pipeline.md` and `frida-pipeline-hardware-breakpoint.md` updated to drop "mermaid diagrams of..." wording. Filename retained for backward compatibility with cross-references.
  - **STATUS.md regenerated** via `findings-status` skill — 41 entries.

MAJOR-8: Transporter power + SpawnCapture mod + RW hub helpers (2026-05-06)
  - **Backfilled 2026-05-06 evening** — original `f1ca67a` commit shipped the work but the TASKS.md Done entry was missed; reconstructed from the commit content and prior handoff.
  - **Transporter power (`tools/frida/mods/powers/Transporter.js`).** `warpEntity(name, x, y, z, roundtrip?)`, `warpPlayer(x, y, z, roundtrip?)`, `clear()`. Mechanism: `oCEntity::setPosition` at vtable slot `+0x50` (RVA `0x6ca7f0`) writes the position field at `+0x324` and broadcasts to subscribers (renderer, camera, minimap). `warpEntity` re-issues setPosition on a 500ms tick interval to defeat the engine's per-frame transform reset (single-shot wasn't enough for arbitrary entities). Player has its own movement controller that respects a single-shot setPosition without ticking, so `warpPlayer` is one-shot. Verified visually: cauldron, teleporter, and boss-spawner-prop all relocated cleanly.
  - **`RW.Player` + `RW.Entity` hub helpers** added to `tools/frida/rw_lab.js`. `RW.Player.{loc, entity, hc, refresh()}` arms a one-shot capture hook at `0x38e260` and lazy-fills on first frame. `RW.Entity.{find(name), list()}` walks the streaming-asset encyclopedia (case-insensitive substring match on settings name; returns `{name, value, entity, loc}`). Encyclopedia walker self-contained in `rw_lab.js` — no dependency on the deprecated `boss_rush` mod (the useful walker logic was ported in directly).
  - **SpawnCapture mod (`tools/frida/mods/spawn_capture.js`) v0.5.0.** Hooks `oCEntity` constructor at RVA `0x6c96f0` (`FUN_1406c96f0`) to capture every entity creation across all subclasses. Three candidate hook targets investigated: `FUN_1406db390` ("addSpawnedEntity") was wrong granularity (per-listener, ~13 listeners per enemy = 393 events for one cauldron fight); `oCEntitySpawner_spawnEntityFromBoundTransform` at RVA `0x6eef40` got 0 captures (cauldrons use a different spawn path); `oCEntity` ctor was the right level (283 captures during one cauldron fight). API: `start()` / `stop()` / `dump()` (raw view by RTTI) / `analyze({ requireComponent? })` (smart filter by initArg-grouped templates).
  - **`oCEntity` component hashmap fully decoded** (from `FUN_1406ca590` decompile). Swiss-Tables hashmap on the entity at offsets `+0x5e8` (ctrl_bytes_ptr), `+0x5f0` (values_array_ptr — 16-byte entries `{u32 hash, u32 pad, void* component}`), `+0x5f8` (count), `+0x600` (capacity_mask), `+0x618` (tombstone_count). Hash uses MurmurHash-style mult by `0xde5fb9d2630458e9`. Empty-hashmap sentinel pattern: entities with no components attached point at a static read-only sentinel inside the binary at `imageBase + 0xedbfa0` for `ctrl_bytes`, with `vals_ptr=NULL`, `count=0`, `mask=0` — distinguishes "no components ever" from "components removed."
  - **Pure-ECS architecture confirmed.** Every entity has the same vtable (RVA `0xf4cc40`); RTTI alone is uninformative. Identity comes from components, not class hierarchy. Component class names of player and enemies catalogued (6 each): `oCDtEntityCpntCharacterController` (`0x1560fd89`), `RegisteredEntitiesHolderEntityCpnt` (`0x1a9a307a`), `oCEntityCpntModifierHolder` (`0x15a04b1a`), `oCDtEntityCpntRemoteDamageOwner` (`0x170176e5`), `oCEntityCpntNetwork` (`0x154fce5c`), plus `oCDtEntityCpntHeroController` (`0x155aac59`) for the player or `oCDtEntityCpntEnemyController` (`0x1561073c`) for enemies — same 6-component shape with a single controller-class swap.
  - **Cauldron spawn mechanic verified.** Spawn anchor for waves = cauldron's current position at the moment each wave fires. Move the cauldron via `Transporter.warpEntity`, the next wave spawns at the new position. Same architecture for the cauldron's barrier entities (barrier offsets are baked for the cauldron's home arena, so they spawn at "wrong" positions when the cauldron is moved to a different area — normal and expected).
  - **Hourglass identity pinned.** `[Entity spawner] NoModel+2Cpnt` IS the chapter hourglass — lives next to Sandman in the chapter's starting arena. Drives the boss-timer leave-arena flip and the early-boss-kill reward chain (`Hourglass_Drop_Bag_*.entity.ot` assets). Caveat-documented in Transporter's header: not blocked, but understand side effects before moving it.
  - **Encyclopedia is the streaming-asset cache**, keyed by asset path. Each settings entry's `+0x48` is a back-pointer to ONE live `oCEntity*` per asset — multiple physical instances of the same asset (e.g., 3-4 teleporters per chapter) share one encyclopedia entry. So `RW.Entity.find("teleporter")` returns one specific teleporter, not the list. EXPERIMENTAL-tagged because it only sees streamed-in cache, not a full per-instance POI list.
  - **Findings layer.** New in-progress `rw/findings/transporter-placement-primitive.md` covers all of the above (component-hashmap layout, ECS confirmation, placement primitive verification, hourglass identity, the 3-candidate ctor hook-target dig, and the SpawnCapture work). Wishlist gained 3 SpawnCapture-related items: snapshot/merge semantics for `start()`, initArg name resolution for `analyze()`, and the open dig "find the global per-instance entity registry."

MAJOR-7: Player teleport primitive verified + Frida lab refactored into powers system (2026-05-06)
  - **Player teleport breakthrough.** Identified `oCEntity_setPosition` at `vtable[+0x50]` (RVA `0x6ca7f0`) as the teleport primitive on the player parent entity (vtable RVA `0xf4cc40`, accessed via `HC[+0x08]` from the `HC_per_frame_update` array). The setter writes the canonical position field at `+0x324` and broadcasts to handler subscribers at `+0x3a8` — visible character snaps on the next render frame. Verified live with multiple deltas: small (+3, +5 — write took, visual change subtle), medium (+15, +20, +50 — clearly visible teleports), and long-range (-200 X, +75 Z — landed in different chapter terrain with real altitude). Off-map teleports work at the position level but the character free-falls (no collision floor outside the playable area). Round-trip recovery via origin-capture works. Corrected the prior session's "+0x1a0 stub" finding — that contract applies to `oITransform3dAccess`-derived classes (e.g. `oCPosRotGo`), not `oCEntity` itself.
  - **Mechanism observations during the dig.** `oCEntity::vtable[+0x48]` returns `+0x324` (position getter); `+0x58` returns `+0x308` (rotation Quat); `+0x68` returns `+0x318` (scale). Engine-managed prev/target Vec3s at `+0x3d8..+0x3ef` (used by `oCEntity_VelocityUpdate_loop` for NPC physics) are NOT required and NOT useful for player teleport — engine resets them to zero within seconds. The natural setPosition caller for the player is different from the velocity-update loop. Fog-of-war does NOT auto-reveal on teleport (movement-bound); POI markers DO reveal on teleport when in proximity (proximity-based).
  - **Findings layer.** `rw/findings/spawn-at-coord-recipe.md` gained a new "Player teleport — verified 2026-05-06" section at the top, oCEntity transform-layout table, and corrected the previous "+0x1a0 stub" framing. Status moved to "in-progress (player teleport — confirmed; spawn step + cross-chapter loading — open)."
  - **rw_lab.js refactored into a hub.** Stripped from ~1300+ lines to 187 — pure infrastructure: mod loader, docstring parser, `RW.help` (terse `HELP TEXT:` output), `RW.after`, `RW.registerMod`, `RW.status`, REPL aliases, boot banner. Self-parses its own source so `help()` lists hub APIs alongside loaded powers. Boot is unconditional: no fall-through into capability code in the file itself.
  - **`mods/powers/` introduced** — six self-contained capability files following an opinionated layout:
    - `Teleport.js` — `refresh()`, `position()`, `to(x,y,z)`, `by(dx,dy,dz)`, `delayTo(s,x,y,z)`, `delayBy(s,dx,dy,dz)`, `toSpawn()`. `refresh` arms a one-shot `HC_per_frame_update` hook that auto-detaches after capture; `setPosFn` cached as `NativeFunction` for zero-alloc per-call. `Teleport.hc` exposed for cross-power borrow.
    - `ChapterBoss.js` — `spawn()`, `delaySpawn(seconds)`, `status()`. `spawn` writes `BossTimer.elapsed = boss_time` so the engine's natural `BossTimer_update` chain fires the arrival event `0x17d8d901` on the next frame. `delaySpawn` is engine-tick (writes `elapsed = boss_time - seconds`; pauses with the game and scales with speedMult — distinct from wall-clock `delay<Verb>`). All BT_* offsets at file scope (domain vocabulary).
    - `Currency.js` — `addShards(n)`. Direct write to `HC+0x1590` (held_dream_shards float32). Captures `Currency.hc` from natural `HC_change_dream_shards` events; falls back to borrowing `RW.Teleport.hc` if Teleport is loaded. The borrow is documented as a known kludge — proper fix is promoting HC capture to a shared utility (`RW.hc`) once a third consumer arrives. Open follow-up: switch to native `HC_change_dream_shards` call with cached `oCCustomFlagList*` for fast HUD animation (see wishlist).
    - `TalentPicker.js` — `force(seed)`, `forceFresh(seed)`, `unforce()`, `count(n)`, `unpatchCount()`, `dumpSlots()`, `dumpPool()`, `clearHeld(idx?)`, `mark(label)`. TLS seed forcing via `NtQueryInformationThread` (GS-free) walking `TEB+0x58` → TLS array → `+0xff3c`. Preserves the `__dyn_tls_on_demand_init` race-handling. Picker-count byte patch sites at `image+0x39c4cd` / `+0x39c4e4`. Marked PARTIALLY VERIFIED — seed forcing isn't exhaustively tested; picker-count byte patch is the best-tested path.
    - `SaveDiagnostic.js` — `probeForBossKillSave()`, `findBuffer()`, `logBuffer(label)`. Marked EXPERIMENTAL. Heap-scans for live `oCDtRootGs`, walks the prep chain on `session_finalize_and_save`, captures buffer state at labeled checkpoints. Carried forward verbatim from rw_lab.
    - `SmokeTest.js` — `run()`. End-to-end exercise: pre-flight auto-loads Currency / Teleport / ChapterBoss, polls 3s for live captures (clear failure msg on timeout), then runs three actions in sequence: Currency.addShards(11), Teleport round-trip via origin-capture + `+30 X` + `to()` snap-back, ChapterBoss.delaySpawn(5). Each action prefixed with `help()` output and a 1-second tick countdown. Pass/fail tally at the end.
  - **Docstring contract** — every public method gets a single block comment immediately above with two `---` fence lines. Fence #1 opens user-facing help (TS-style signature line + concise prose: requirements / usage / result / caveats). Fence #2 closes help and opens MECHANISM (developer-only detail: RVAs, decompile reasoning, byte writes — invisible to `help()`). The parser scans `/* */` blocks for fence pairs; blocks without them are treated as ordinary block comments. Convention details + examples in `tools/frida/CODE_STANDARDS.md`.
  - **`tools/frida/CODE_STANDARDS.md` added** — opinionated rules covering: project layout (powers vs mods), naming (PascalCase power files / camelCase methods / SCREAMING_SNAKE_CASE for offsets+RVAs+tunings / `delay<Verb>(seconds, …sameArgs)` for delay variants), the docstring contract, where named values live (file-scope vs method-local cohesion rule), state and re-load safety, hook ownership, cross-power state sharing (the borrow pattern with strict rules), shared utilities, top-level REPL aliases, log prefix, and a per-power validation checklist. CLAUDE.md gained a "Frida: code standards" pointer.
  - **`RW.after(seconds, fn)`** — shared wall-clock delay used by powers' `delay<Verb>` helpers. Validates seconds (non-negative number) + fn (function). Standardizes seconds-as-input + arg validation so each power's delay variant is a one-liner. Engine-tick delays (e.g. `ChapterBoss.delaySpawn`) are explicitly distinct and don't use `RW.after`.
  - **`hello.js` updated** to test the new hub APIs (`loadMod`, `loadPower`, `help`, `RW.after`) instead of the dead globals (`forceBossSpawn`, `bossTimerStatus`, `bossTimerSetElapsed`).
  - **Wishlist follow-ups added**: decode `oCEntitySceneContext +0x520` array structure (gates POI / dev-console), find and teleport to active quest POI (depends on the prior), native `HC_change_dream_shards` call for fast HUD animation.
  - **Cross-chapter boss-rush concluded infeasible.** The engine loads exactly one chapter's content at a time; entering a new chapter replaces the prior. No background queue. The cold path-based loader crashes (no engine-side load-context). Encyclopedia in chapter 1 only holds chapter-1 spawner variants. The original "fight Map Bosses 1→2→3→(4) back-to-back without transitions" goal is structurally impossible. `ChapterBoss.spawn` collapses the chapter-end iteration loop instead — useful, smaller scope. Wishlist entry updated to mark the original goal closed; chapter-end suppression remains the open piece for any single-chapter variant.


  - **C5 of PRP-7 Phase 2 (steps 13–14 backend).** Dev Console PTY backend lit end-to-end. State machine, mode-swap kill semantics, and Frida-PTY smoke harness all wired. Step 14's user-side gate (Frida attach to running Ravenswatch.exe) is the next manual milestone before C6 builds the real Dev Console.
  - **`src-tauri/src/state.rs`** — managed state for the Dev Console (Superpowers only). `Mode` (`rerw|frida`), `State` (`idle|starting|running|swapping|stopping|exited`), `TerminalState` (the snapshot serialized to the frontend; `pending_mode_swap` is `#[serde(skip)]` so it's backend-only). `ManagedTerminal` holds the scalar state + dimensions + child resources behind separate `tauri::async_runtime::Mutex` guards: one per long-lived resource per PRP §"State synchronization", so a brief reader-thread access doesn't block a kill from another task. Default `Mode::Frida` reflects the primary Superpowers use case (REPL against a running Ravenswatch); the dev console specifically exists for Frida work, and rerw-via-CLI work has the Build Tester wizard.
  - **`src-tauri/src/terminal.rs`** — the eight `terminal_*` IPC commands plus their support machinery. `terminal_get_state`, `terminal_set_mode`, `terminal_start`, `terminal_stop`, `terminal_restart`, `terminal_write_input`, `terminal_resize`, `terminal_set_frida_script`. State machine implemented per PRP §"Console mode swap": same-mode requests emit a resync `terminal-state` event without respawning (frontend-remount artifact, not a restart intent); idle/exited + other-mode atomically swaps with divider line; running + other-mode transitions to Swapping, takes the killer via `ChildKiller::clone_killer()`, calls `kill()` (Linux SIGKILL / Windows TerminateProcess — no grace), and schedules a 5s polling-timeout demotion task that fires `[ravensmith] terminal child did not exit; mode swap aborted.` if the wait task hasn't completed the swap by then. **Busy-rejection rule honored**: `starting | stopping | swapping` returns `Err(...)` with no parallel `app`-stream emit, so the frontend never double-reports. `terminal_start` constructs argv per mode — Frida builds `["-n", "Ravenswatch.exe", "-l", <selected_script>]` from backend state and **ignores caller args** per PRP §"args semantics by mode"; rerw passes args verbatim. `PYTHONUNBUFFERED=1` and `FORCE_COLOR=1` set on every spawn (PTY path), same rule as `cli::rerw_run`. Resize-after-spawn discipline: `terminal_resize` updates the size mutex and calls `MasterPty::resize()` if a master is alive; the size mutex is read at next spawn so dimensions survive mode swap. `kill_on_close()` exposed for `lib.rs`'s `WindowEvent::CloseRequested` wiring.
  - **Cross-task sharing.** Tauri's managed state is `Arc<ManagedTerminal>` (typedefed `SharedTerminal` in terminal.rs). Handlers receive `TauriState<'_, SharedTerminal>` and call `mt.inner().clone()` to get an owned Arc that moves into background tasks. Reader task: `tauri::async_runtime::spawn_blocking` loops on `reader.read(buf)`, decodes UTF-8 lossy (PRP-acknowledged: rare partial-codepoint boundary lossy-decodes are acceptable for V1 ASCII-or-well-formed-UTF-8 traffic), emits `terminal-output { stream: "pty", data }`. Wait task: `spawn_blocking` blocks on `child.wait()`, gets exit code, hops back into async via nested `spawn` to update Mutex-guarded state, emits `terminal-exit { code }` then transitions to Exited (or completes the swap to Idle of new mode if `pending_mode_swap` was stashed). Resources (killer, writer, master) cleared after state transition.
  - **Sidecar path resolution.** `resolve_sidecar_path()` tries the bundled location (next to the main exe — what Tauri's bundler produces) first, then falls back to `<src-tauri>/binaries/<name>-<triple><suffix>` for `tauri dev`. Triple is fixed at compile time via `cfg!(target_os = ...)`; suffix is `.exe` on Windows. Tauri-plugin-shell's path resolver isn't exposed publicly, hence the manual lookup — PTY-spawn bypasses the shell plugin entirely (it yields plain pipes, not PTY semantics).
  - **`lib.rs` setup closure.** Bundled `rw_lab.js` resolved via `app.path().resolve("resources/rw_lab.js", BaseDirectory::Resource)` then `Path::exists()` probed. **Three-arm soft-fail** per PRP §step 13: resolve+exists → `selected_script = Some(<path>)`; resolve OK but file missing → None + queued app-stream warning `[ravensmith] bundled rw_lab.js not found at <path>; pick one via Frida > Open Script…`; resolve errs → same soft-fail + similar warning. `app.manage::<SharedTerminal>(Arc::new(mt))` runs unconditionally; `setup` returns `Ok(())` always — never `?`-propagates per PRP "the worst dev-experience failure mode in the proposal" (panic with no UI). `WindowEvent::CloseRequested` calls `terminal::kill_on_close(mt)` so app shutdown does not leak an orphan sidecar. Handler list split: default mode registers only `cli::rerw_run`; superpowers adds the eight `terminal_*` handlers.
  - **`Cargo.toml`** — `portable-pty 0.9.0` is now actually used (was optional from C2 with no consumer). Default-features build doesn't pull it (verified: `cargo check` builds fast; `cargo bloat` gate becomes meaningful in C8).
  - **Frontend smoke harness.** `src/views/Smoke.vue` renders xterm.js with FitAddon, listens to `terminal-output` (CRLF-normalized via the idempotent `s.replace(/\r?\n/g, "\r\n")` regex from PRP §caveats — handles LF-only and existing CRLF without doubling) and `terminal-state` events, forwards keystrokes to `terminal_write_input`, calls `terminal_resize` on init + on window resize, exposes Start/Stop buttons bound to `terminal_start({ args: [] })` / `terminal_stop`. Default mode is Frida + bundled `rw_lab.js` pre-selected, so Start spawns Frida directly without needing a mode swap or picker click. Route registered at `/smoke` under `__SUPERPOWERS__` only — joins DevConsole.vue placeholder. **Smoke.vue is deleted in C6** when the real Dev Console replaces it (per PRP step 14: "exists only to prove PTY+Frida fit without the surrounding mode-swap UI in the way").
  - **Bundle visualization gate now live.** Default-build `dist/stats.json` contains zero xterm references; superpowers-build contains 7. Asymmetry between the two runs is the PRP-acceptance signal that DevConsole + xterm are not in the default chunk graph. Default JS chunk: 174 KB (unchanged from C4 — Smoke/DevConsole are dynamic-imported). Superpowers: index 176 KB + DevConsole 0.32 KB + Smoke 345 KB (xterm + addon-fit).
  - **Validation green.** `cargo check` × 2 (~1s warm). `cargo clippy -- -D warnings` × 2 — both clean. `cargo fmt --check` — applied + verified. `bun run build` × 2 — clean (44/47 modules respectively, ~300ms each).
  - **Manual smoke gate (user-side, PRP step 14) — DEFERRED.** Attempted in WSL2 Linux against Ravenswatch.exe running on the **Windows host** — fails because WSL2 is a separate VM and Linux-host Frida cannot reach Windows-host processes (different OS, different process tables). This was always a structural blocker for *this* user's environment, not a bug in C5: the PRP-7 §"Frida attach policy → Linux" path assumed Ravenswatch under Steam Proton on the *same* Linux host, not Steam-on-Windows. Step 14 stays deferred until either (a) a Windows-native Tauri dev loop is set up (PRP-7 §setup.md "Windows section" — winget install rustup/bun/uv/just + WiX + MSVC build tools, ~30 min), or (b) Ravenswatch runs under Proton inside the WSL VM. User has chosen "neither yet" — V1 work paused at C5 with backend code compiling clean both feature configs, no shipping anyway. Future-resume note: the Frida sidecar binary itself works (smoke proved this — `Frida 17.9.6 - A world-class dynamic instrumentation toolkit` banner + `Connected to Local System` line printed before the by-name lookup failed), so PyInstaller bundling is sound; only the cross-VM attach is blocked.
  - **WSLg dev-loop quirks worth documenting before next session.** Tauri windows in WSLg+wry exhibit two reproducible glitches that are noise, not bugs: (1) initial window position can land off-screen on multi-monitor setups due to Weston's Wayland output mapping vs. Windows multi-monitor virtual coordinates — `center: true` in `tauri.conf.json` (kept in this commit) helps; if the window still spawns off-screen, change the window `title` once to bust the Windows-side per-app position cache. (2) Cursor disappears in title bar (and sometimes whole window) under wry+Weston — clicking outside then back into the window re-acquires the cursor surface. `GDK_BACKEND=x11` *worsens* the cursor issue and *does not* help with sizing for this hardware, despite being the conventional WSLg workaround — leave Wayland as the default.

MAINT-40: Ravensmith Build Tester stub + rerw_run (2026-05-05)
  - **C4 of PRP-7 Phase 2 (steps 11–12).** End-to-end pipe lit: Vue → Tauri IPC → tauri-plugin-shell sidecar → bundled rerw PyInstaller binary → output rendered with ANSI colors and CR-collapsed spinners. The probe step in the V1 stub wizard exercises the full path; running `--version` round-trips through the sidecar and renders `rerw 0.2.0` (or whatever the bundle is built against) in the output region.
  - **Rust: `cli::rerw_run`.** New `src-tauri/src/cli.rs` implements the request/response sidecar bridge per PRP §"IPC Boundary → Build Tester". Args: `args: Vec<String>`, `timeout_ms: Option<u64>`. Spawns via `app.shell().sidecar("rerw")` (Tauri shell plugin's external-bin lookup), sets `PYTHONUNBUFFERED=1` + `FORCE_COLOR=1` envs unconditionally on every spawn (Click strips ANSI when stdout is not a TTY; Python block-buffers stdout without `-u` — both regress silently otherwise). Drains `tauri-plugin-shell::process::CommandEvent::{Stdout,Stderr,Terminated,Error}` into `CommandResult { stdout, stderr, exit_code }` via a tokio mpsc receiver. `tokio::time::timeout(deadline, drain_loop)` caps any single invocation at 30s default (`DEFAULT_TIMEOUT_MS`); on expiry, explicit `child.kill()` then `Err("rerw_run: timed out after Nms")` — dropping `rx` does NOT terminate the child, so the kill is load-bearing per PRP §"Timeout contract". `tokio = { version = "1", features = ["time", "macros"] }` added as a direct dep — tauri pulls tokio transitively but does not re-export `tokio::time::timeout` from `tauri::async_runtime`.
  - **`lib.rs` re-wired.** Drops scaffold's demo `greet` command. Plugs `tauri_plugin_shell::init()` + `tauri_plugin_dialog::init()`, registers `cli::rerw_run` as the sole handler for both feature configs (cfg-gated split + `mod terminal;` arrive in C5 with the actual terminal module). The `mod cli;` declaration is unconditional — Build Tester ships in default builds, so `rerw_run` must be reachable without the superpowers feature.
  - **Frontend: typed invoke wrappers.** `src/lib/api.ts` exports `rerwRun(args, timeoutMs?): Promise<CommandResult>` mirroring the Rust signature one-to-one. **Snake_case keys** in the invoke payload (`{ args, timeout_ms }`) match the Rust function parameters verbatim — Tauri v2's `#[tauri::command]` uses serde's default field naming; sending camelCase from JS would silently drop `timeoutMs` and the 30s default would always apply. `CommandResult` interface mirrors `cli::CommandResult` field names exactly.
  - **`lib/rerw.ts` — output rendering.** `collapseCR(raw)` splits on `\n`, keeps only the segment after the final `\r` per line — handles display_lib's spinner rewrites (`\r<frame>`) so the user sees one final frame, not hundreds stacked. `renderRerwOutput(raw)` chains `collapseCR` → `ansi-to-html` → `\n → <br>`. `ansi-to-html` configured with `escapeXML: true` (XSS gate — rerw output may contain user data with `<`/`&`), `newline: false` (we do our own `<br>`), foreground `#e5e7eb` (slate-200) for dark-mode legibility, `bg: transparent`.
  - **`StatusStrip.vue`.** Probes `rerwRun(["--version"], 5000)` on mount, renders `checking…` → `resolved · rerw 0.2.0` (emerald) or `missing · <reason>` (rose). Lives in `App.vue`'s footer per PRP shell layout, not inside any view, so it's visible across both Build Tester and (future) Dev Console.
  - **`Wizard.vue` — generic step harness.** Props-driven (`steps: { title: string }[]`), tracks `currentIndex`, exposes `back()` / `next()` via `defineExpose`. Renders an ordered step indicator with three visual states (current emerald, completed slate-500 muted, upcoming slate-400) plus a body slot that receives `{ index, step }`. The "intentionally generic" rule from PRP §Notes: same harness powers V1 stub and the eventual real wizard with no rework — only the per-step bodies change.
  - **`BuildTester.vue` rewrite.** Five-step wizard ("Choose hero / chapter / level → Review → Apply"), first four are placeholders, fifth is the wired probe. Renders `rerwRun(["--version"])` result through the four PRP-specified output states: (1) exit_code 0, stderr empty → stdout only, (2) exit_code 0, stderr non-empty → stdout + divider + stderr, (3) exit_code non-zero → rose banner `rerw exited with code <N>` + stdout + stderr, (4) timeout → rose banner `rerw timed out after 30 s · child killed`. The step's "Run probe" button toggles to "Re-run" after a successful probe, "Running…" while in flight.
  - **Validation green.** `cargo check` (default + superpowers, both ~5s after warm cache). `cargo clippy -- -D warnings` (× 2 feature configs) — both clean. `cargo fmt --check` — initial run flagged a multi-line `?`-chain in `cli.rs` that should fit one line; `cargo fmt` applied the fix; re-check passes. `bun run build` (default) — 44 modules, 174 KB JS chunk (jumped from 87 KB in C2 due to ansi-to-html + StatusStrip + Wizard). `VITE_SUPERPOWERS=true bun run build` — 46 modules, 175 KB index + 0.32 KB DevConsole chunk. DevConsole tree-shake holds: separate chunk under Superpowers only.
  - **Manual smoke gate (user-side).** Running `bun run tauri dev` from `app/ravensmith/` should open a window with the wizard; clicking through to step 5 and pressing "Run probe" should display `rerw 0.2.0` in the output region. Status strip footer should show `rerw sidecar: resolved · rerw 0.2.0`. Not run this session — needs WSLg / X.

MAINT-39: Ravensmith sidecar build pipelines (2026-05-05)
  - **C3 of PRP-7 Phase 2 (steps 9–10).** Two PyInstaller sidecar build scripts landed at `scripts/build-{rerw,frida}-sidecar.sh`. Both detect host triple via `uname -s` (`Linux*` → `x86_64-unknown-linux-gnu`, `MINGW*/MSYS*/CYGWIN*` → `x86_64-pc-windows-msvc.exe`) and produce target-triple-named binaries in `app/ravensmith/src-tauri/binaries/` per Tauri v2's `externalBin` resolution contract. PyInstaller cannot cross-compile, so the host that runs the script *is* the bundle target — same recipe runs on both Linux and Windows hosts.
  - **rerw sidecar.** Bundles `tools/rerw-src/cli.py` via `tools/rerw-src/.venv/bin/pyinstaller` (the project's own dev venv from MAINT-36). `--add-data data:data` ships `data/save-fields.yaml` + `data/heroes/` + magical-/powerup-items YAML so `lib.save_fields.load_fields()` resolves at runtime. `--collect-all display_lib repl_lib tree_lib prompt_toolkit` absorbs the workspace-editable shared-lib gotcha (PRP §"caveats: PyInstaller workspace-editable deps") plus `prompt_toolkit` which `repl_lib` imports indirectly — surfaced as `ModuleNotFoundError: No module named 'prompt_toolkit'` on the first `--version` smoke run, exactly the failure mode the PRP caveat warned about. `--copy-metadata rerw-src` ships the package metadata so `cli.py:_read_version()`'s `importlib.metadata.version("rerw-src")` call resolves (without it, `--version` raises `PackageNotFoundError`). 19 MB output, smoke test `--version` answers `rerw 0.2.0` cleanly.
  - **Frida sidecar.** Throwaway uv venv at `.run/frida-sidecar-build/` (gitignored — `.run/` is repo-wide scratch space) keeps `frida-tools` out of any permanent dev environment. Installs `frida-tools>=13` + `pyinstaller`, writes a 2-line `frida_entry.py` that calls `frida_tools.repl:main` (PyInstaller bundles scripts, not console entry points), runs `--collect-all frida frida_tools prompt_toolkit pygments` per PRP §"caveats: PyInstaller-bundling frida-tools likely needs --collect-all". Output renamed to `frida-x86_64-unknown-linux-gnu` (55 MB), smoke test answers `17.9.6` for the Frida REPL version. Linux native-Frida → Wine/Proton attach is the unverified Step 14 risk gate; this commit only proves the bundle runs and answers `--version`, not that it can attach to Ravenswatch under Proton.
  - **Bundled default Frida script staged.** `scripts/build-frida-sidecar.sh` copies `tools/frida/rw_lab.js` (65 KB source-of-truth) to `app/ravensmith/src-tauri/resources/rw_lab.js`. The Superpowers overlay's `bundle.resources: ["resources/rw_lab.js"]` (from C2) references the staged copy via a relative-to-`src-tauri/` path. Edits to the source `tools/frida/rw_lab.js` require re-running this script + restarting the dev console — Tauri copies bundle.resources at build time, not at runtime.
  - **`install -m 755`, not `cp`.** First rerw build failed on the smoke test with `Permission denied` — `cp` overwrites contents but inherits destination permissions, and the C2 0-byte stub (`-rw-r--r--`) was non-executable. Both scripts now use `install -m 755 dist/<name> <target>` for the binary and `install -m 644` for the rw_lab.js stage, which always sets perms regardless of any prior file at the destination.
  - **`tools/rerw-src/.gitignore` added.** PyInstaller emits `build/<name>/` and `<name>.spec` in the directory it runs from. `dist/` is already covered by the root gitignore; `build/` and `*.spec` were leaking. Tight rerw-src-scoped ignore added — frida sidecar runs in `.run/`, so its transient files are already covered.
  - **Validation green.** Both PyInstaller bundles answer `--version`. `cargo check` (default + `--features superpowers`) clean — the real PyInstaller-built `rerw-x86_64-unknown-linux-gnu` (19 MB executable) replaces C2's 0-byte stub at the exact path Tauri's build script validates, so `externalBin: ["binaries/rerw"]` resolves with no surprises. Step 14 (Frida-PTY attach to Ravenswatch.exe under Proton) remains the manual gate; this commit only validates the static bundle shape.

MAINT-38: Ravensmith mode-gating + capabilities (2026-05-05)
  - **C2 of PRP-7 Phase 2 (steps 4–8).** Compile-time mode split landed end to end. Both gating sides set up but no Superpowers code paths yet — terminal module, xterm imports, and Dev Console UI arrive in C5/C6.
  - **Cargo feature flag.** `Cargo.toml` adds `[features]` block with `default = []` and `superpowers = ["dep:portable-pty"]`. Versions pinned to PRP-7 §"Pinned Versions": `tauri-build 2.6.0`, `tauri 2.11.0`, `tauri-plugin-dialog 2.7.1`, `tauri-plugin-shell 2.3.5`, `thiserror 2`, `portable-pty 0.9.0` (optional). The optional `dep:` syntax means default builds never compile portable-pty — verified via `cargo bloat` is C8 work, but `cargo check` × 2 (with and without `--features superpowers`) green here.
  - **Vite mode flag.** `vite.config.ts` reads `process.env.VITE_SUPERPOWERS` directly (Vite's `loadEnv()` only reads `.env*` files, not `process.env`) and emits a `__SUPERPOWERS__` `define`. `src/vite-env.d.ts` declares the global so vue-tsc passes. Bundle visualizer wired with two plugin invocations (`template: "treemap"` → `dist/stats.html` for humans + `template: "raw-data"` → `dist/stats.json` for the acceptance grep gate). The `json: true` shorthand was deprecated in `rollup-plugin-visualizer` 5.13 — needed two instances to get both outputs.
  - **Router + view scaffold.** `src/router.ts` registers `/` → `BuildTester.vue` always; `/dev` → dynamic-import `DevConsole.vue` only when `__SUPERPOWERS__` is true. The function-form `() => import("./views/DevConsole.vue")` is the load-bearing tree-shake signal — replacing it with an eager import would break the chunk-graph contract. `src/main.ts` wires the router via `createApp(App).use(router).mount("#app")`. `src/App.vue` rewrites the scaffold demo into a shell with header / nav / RouterView / footer; the Dev Console tab is gated by `v-if="superpowers"` (a script-setup const that captures the build-time `__SUPERPOWERS__` value, since Vue templates can't see globally declared TS symbols). Both views are placeholder stubs — Build Tester wizard and Dev Console wiring land in C4 and C5/C6 respectively.
  - **Capabilities + base config.** `capabilities/default.json` rewritten with the PRP baseline: `core:default`, `dialog:allow-open`, plus three scoped shell grants for the rerw sidecar (`shell:allow-execute`, `shell:allow-spawn`, `shell:allow-kill` — each with `{"name": "binaries/rerw", "sidecar": true}`). Wildcards forbidden by Tauri v2 capability syntax; the `name` matches the `externalBin` entry in `tauri.conf.json` ("binaries/rerw"), not the basename. **No `capabilities/superpowers.json`** — files in `capabilities/` always auto-load and would leak Superpowers grants into default builds; the Superpowers capability is defined inline within the overlay file (below). `tauri.conf.json` updates: `productName: "Ravensmith"` (capitalised), `bundle.targets` removed (per-host overlay does this), `bundle.externalBin: ["binaries/rerw"]` added, `app.security.capabilities: ["default"]` added (explicit listing replaces auto-load — verified against `.ai/docs/tauri-v2/capabilities.md`).
  - **Platform overlays.** `tauri.linux.conf.json` (`bundle.targets: ["appimage"]`) and `tauri.windows.conf.json` (`bundle.targets: ["msi"]`) — Tauri v2 auto-merges these per host, so `cargo check` from Linux exercises the AppImage configuration without WiX or AppImageKit ever needing to run on the wrong platform. `tauri.overlay.superpowers.json` (NOT auto-loaded, `-c`-merged only) re-lists `binaries/rerw` + adds `binaries/frida` to `externalBin`, declares `bundle.resources: ["resources/rw_lab.js"]`, and re-lists `"default"` alongside an inline `superpowers` capability with empty `permissions`. Re-listing is required because Tauri v2's array-merge for `app.security.capabilities` is replace-semantics — the base's explicit `["default"]` plus the overlay's `["default", { inline superpowers }]` produces the correct final list. Same logic applies to `bundle.externalBin` (re-list `binaries/rerw` so the default sidecar survives the array replace).
  - **Sidecar dirs scaffolded.** `src-tauri/binaries/` + `src-tauri/resources/` created with their own `.gitignore` (everything ignored except `.gitignore` / `.gitkeep` / `README.md`) plus `.gitkeep` placeholders. `binaries/README.md` documents the per-host triple naming convention (`rerw-x86_64-unknown-linux-gnu`, `rerw-x86_64-pc-windows-msvc.exe`, etc.), the `just ravensmith-build-*-sidecar` invocations that produce them, and why the binaries are not committed (per-host PyInstaller artifacts, 10–50 MB each). A 0-byte `binaries/rerw-x86_64-unknown-linux-gnu` stub exists locally so `cargo check` (which now sees `externalBin: ["binaries/rerw"]` in the base config and validates the per-host file's existence at build-script time) passes — the stub is gitignored and gets overwritten with the real PyInstaller bundle by C3's `scripts/build-rerw-sidecar.sh`.
  - **Scaffold demo assets removed.** `public/vite.svg`, `public/tauri.svg`, `src/assets/vue.svg` deleted (previously committed in C1) — the rewritten `App.vue` no longer references them. `index.html` updated: title `Ravensmith`, the `<link rel="icon" href="/vite.svg">` line dropped.
  - **Validation green.** `cargo check` (4.24s, default features) + `cargo check --features superpowers` (29.12s; pulls webkit2gtk + tao + muda + wry checks since the feature flag is wired to portable-pty and pulls more transitive surface). `bun run build` (default, 25 modules → 1 JS chunk, 85.71 KB) + `VITE_SUPERPOWERS=true bun run build` (27 modules → 2 JS chunks: `index-*.js` 87.04 KB + `DevConsole-*.js` 0.32 KB). The chunk asymmetry confirms tree-shaking works: DevConsole.vue is a separate chunk only when Superpowers is on. `dist/stats.html` (168 KB human treemap) + `dist/stats.json` (13 KB raw module data) both emit. xterm grep returns 0 in both modes today — meaningful asymmetry only fires once C5/C6 imports xterm into DevConsole.vue.

MAINT-37: Scaffold ravensmith Tauri app (2026-05-05)
  - **C1 of PRP-7 Phase 2 (steps 1–3).** Tauri v2 + Vue 3 + TypeScript app scaffolded at `app/ravensmith/` via `bunx create-tauri-app ravensmith --manager bun --template vue-ts --identifier dev.ravensmith.app --tauri-version 2 -y` (`create-tauri-app 4.6.2`, matches PRP pin). Layout: `package.json` / `bun.lock` (text format), `index.html`, `src/{App.vue,main.ts,style.css,assets,vite-env.d.ts}`, `vite.config.ts`, `tsconfig*.json`, `src-tauri/{Cargo.toml,Cargo.lock,tauri.conf.json,build.rs,capabilities/default.json,icons/,src/{main.rs,lib.rs}}`. Scaffold writes `lib.rs` (not `main.rs`) as the run entry point — matters for the cfg-gated handler list landing in C2. Public `app/docs/ravensmith/` (sibling, written in MAINT-36) untouched.
  - **Pin sweep (npm/bun deps).** `package.json` rewritten against the PRP "Pinned Versions" table with exact pins (no `^` / `~`): `@tauri-apps/api 2.11.0`, `@tauri-apps/plugin-dialog 2.7.1`, `@tauri-apps/plugin-shell 2.3.5`, `@xterm/addon-fit 0.11.0`, `@xterm/xterm 6.0.0`, `ansi-to-html 0.7.2`, `vue 3.5.33`, `vue-router 5.0.6`, `@tauri-apps/cli 2.11.0`, `@tailwindcss/vite 4.2.4`, `@vitejs/plugin-vue 6.0.6`, `rollup-plugin-visualizer 5.13.1`, `tailwindcss 4.2.4`, `typescript 6.0.3`, `vite 8.0.10`, `vue-tsc 3.2.8`. `packageManager: bun@1.3.13` declared. `bun install` clean (126 packages, ~1s) → `bun.lock` regenerated against the pinned set. Build script normalized to `vue-tsc && vite build` per PRP. Cargo deps left at `tauri 2 / tauri-build 2 / serde 1 / serde_json 1` for C2 to lift to PRP pins (where the feature flag + plugin deps land).
  - **`tauri-plugin-opener` removed wholesale.** Scaffold default included `@tauri-apps/plugin-opener` (frontend) + `tauri-plugin-opener` (Cargo) + `.plugin(tauri_plugin_opener::init())` (`lib.rs`) + `opener:default` permission grant (`capabilities/default.json`). Not in PRP scope; all four references dropped. Without the capability fix the build script fails with `Permission opener:default not found, expected one of core:default …` even though the Rust `init()` call is gone — the capability scanner runs against the JSON regardless of whether the plugin is loaded.
  - **Tailwind v4.** `@tailwindcss/vite` + `tailwindcss` added (devDeps). `vite.config.ts` adds the `tailwindcss()` plugin alongside `vue()`. `src/style.css` is a single `@import "tailwindcss";` line; `src/main.ts` imports `./style.css` so the host page picks it up. The PRP-required `__SUPERPOWERS__` define + `rollup-plugin-visualizer` wiring stays for C2 (mode-gating commit) — adding it here would require the conditional dynamic-import hooks that don't exist yet.
  - **Icon source moved to tracked path.** `rw/dumps/ravensmith.png` (gitignored 1254×1254 PNG) → `app/ravensmith/assets/icon-source.png` (tracked) per `CLAUDE.md § Scripts: dumps vs scripts` ("the moment a script is invoked from documentation, move it" — same logic for assets referenced by the build pipeline). `bun run tauri icon assets/icon-source.png` regenerated the desktop set (`32x32.png`, `128x128.png`, `128x128@2x.png`, `icon.icns`, `icon.ico`) plus iOS/Android/Windows-Store variants. Mobile/store variants are emitted but not bundled by `tauri.conf.json:bundle.icon`.
  - **`productName` capitalised** to `"Ravensmith"` in `tauri.conf.json` per PRP.
  - **Validation green.** `cargo check` (first run, 17s after dep download) — `Cargo.lock` written (4611 lines, committed per PRP "app, not library" rule). `bun run build` (vue-tsc + vite v8) — clean, 183ms, 63KB JS chunk. `bun.lock` regenerated by `bun install`. No clippy/test gates run yet; those land in C2 (cfg-gated terminal module + check recipe).

MAINT-36: Ravensmith app setup — Void prereqs (2026-05-05)
  - **`app/docs/ravensmith/setup.md` seeded.** Documents Phase 1 host prereqs for building the Ravensmith Tauri app: Rust toolchain via rustup (≥1.77.2; verified with 1.95.0), `cargo-bloat` (acceptance gate dep), Void system libs for Tauri's webview + AppImage tooling (`libwebkit2gtk41-devel`, `libayatana-appindicator-devel`, `librsvg-devel`, `openssl-devel`, `base-devel`, `xdotool-devel`, `xdg-utils`, `fuse`, `fuse3`), and `pyinstaller>=6.10,<7` as a `tools/rerw-src` dev dep. Void package map confirmed against `xq-api.voidlinux.org` 2026-05-05 — Void's naming diverges from Tauri's official Debian/Arch/Fedora prereqs page (e.g. `libwebkit2gtk41-devel` not `libwebkit2gtk-4.1-dev`; `xdotool-devel` not `libxdo-dev`). Scope note clarifies the prereqs are required only for Tauri-app work, not for `rerw` CLI / RE / Frida-lab work that the rest of the repo houses.
  - **PyInstaller dev dep landed.** `tools/rerw-src/pyproject.toml` adds `pyinstaller>=6.10,<7` to the dev group (verified via `uv add --group dev`); `uv.lock` regenerated. PRP-7's `scripts/build-rerw-sidecar.sh` (step 9, Phase 2) consumes this through the rerw venv.

MAINT-35: Drop PLANNING.md tracking system (2026-05-05)
  - **Removed PLANNING.md from the planning system.** Three files deleted: `.ai/planning/templates/PLANNING_TEMPLATE.md` (template itself), `.claude/commands/convert-planning.md` (slash command existed only to produce a PLANNING.md), `.ai/planning/prp/templates/prp_bulk.md` (designed for `/generate-prp` Mode 1's one-PRP-per-Work-Table-row flow; orphan without Mode 1).
  - **Slash command surface trimmed.** `/generate-prp` reduced from two modes (PLANNING.md bulk + proposal) to proposal-only; description and front matter rewritten. `/execute-prp` no longer adds a Work Table row at execution time. `/prime-prp-workflow` drops the PLANNING.md key-files entry. The PRP standalone template's success-criteria checklist drops the PLANNING.md cross-reference.
  - **Docs follow-up.** `.ai/AGENTS.md` drops the "PLANNING.md Work Table Rules" section + the planning-template/active-planning file-location entries. `docs/agentic-workflow/development-cycle.md` drops the WP-Codes (Work Packages) section, the PLANNING.md references in §Task Codes, §Direct execution workflow, and §Idempotency. `docs/agentic-workflow/README.md` drops `/convert-planning` from the slash-command table and the PLANNING.md row from the key-files table.
  - **Scafcli scaffold contract updated.** `tools/scafcli-src/data/manifest.json` planning module loses `PLANNING_TEMPLATE.md`, `convert-planning.md`, `prp_bulk.md`, and the `.ai/planning/prd/` dir entry. `tools/scafcli-src/tests/test_assembler.py::test_required_dirs_created` drops the matching `prd/` assertion. New repos scaffolded by scafcli no longer get PLANNING.md infrastructure. Full scafcli test sweep green (104 passed).

MAINT-34: Clean-save lab-source rule + Tauri v2 PRP docs (2026-05-05)
  - **Clean-save lab-source rule codified.** `CLAUDE.md` gains "Never source labs from the clean proof" — clean saves lack records required for player-info edits (no item records, no Nightmare Keys record), so the byte shape triggers `rerw` writer bootstrap failures. Broader rule documented in `rw/docs/workflow/save-editing.md` §"Source must already contain the record type you want to edit": the writer mutates existing records but cannot bootstrap a brand-new record into a save that has none of that type. Failure-mode list covers items, Nightmare Keys, and likely others (talents, hero-specific blocks). Workaround: pick a source proof from a run that has used / picked up the resource. Lab-planning rule: clean saves and very-early-chapter saves are weak lab sources; prefer mid- or late-run proofs where most record types are already populated.
  - **Tauri v2 PRP docs.** `.ai/docs/{bun,tauri-v2,MANIFEST.md}` — vendored documentation pulls grounding the future Tauri v2 desktop-app PRP listed in the wishlist (Build Tester default + dev-superpowers gated build). Each pull records source URL + UTC timestamp + sha256 of the local summary file. Tauri-v2 docs cover capabilities, CLI, config, plugin-dialog, plugin-shell, sidecar; bun coverage is a single lockfile-format reference.

MAINT-33: Multiplayer host-authority + items-budget-rule-a findings (2026-05-04)
  - **`rw/findings/multiplayer-host-authority.md` (in-progress).** Runtime MP architecture mapped end-to-end. Stack: Stormancer (Nacon middleware) for cross-platform brokerage / matchmaking / RPC / party + Steamworks for lobby + EOS for cross-platform lobby/NAT-punch/relay + EOS RTCAudio for voice + RakNet (with SLikeNet fork) for P2P transport, wrapped behind a Stormancer transport abstraction. Engine entity replication via RakNet `ReplicaManager3` wrapped by `oINetworkReplicationManagerSceneContext` (game-state root: `LobbyGs`). Topology confirmed via build-path strings (`C:\Workspace\oengine2\ThirdParty-Ext\RakNet\…`, `C:\ravenswatch\ravenswatch\ThirdParty-Ext\Stormancer\…`). Three-mode replication enum (host-only / replicated / client-only) at `oCSLNetReplicationManagerSceneContext` vtable. Host-authority confirmed for shard wallet via `forceBossSpawn` host-side experiment + `HC_change_dream_shards` canonical write path: host writes `HC+0x1590` raw → peer's per-frame `HC_per_frame_update` watcher detects and dispatches → replication propagates to all peers. `+0x1590` is the raw shard wallet field; `+0x1d48` and `+0x1d78` are runtime stat-tracker pointers (cumulative-earned / cumulative-spent). `+0x15d8` is a subscriber list head useful for enumerating shard-change consumers (Hope Diamond, Heal-on-gain, achievement counters).
  - **`rw/findings/items-budget-rule-a-investigation.md` (in-progress).** Rule A fresh-reference-allocation cap investigation, stopped 2026-05-04 with no actionable save-side fix. Two distinct failure modes share crash hash `276109f4-3a0d-29ee-1ac0-fc5b348ff902`: documented Rule A (deserialize-time fresh-counter cap, crashes on load) and a separately-characterized cross-type reuse-counter corruption (saves built with `--reuse-counter-from-slot N` where N's slot type differs from the new item's type; saves load cleanly but crash on first action that reads the corrupted entity). The corruption is GUID-merge-driven: `hero_inventory_create_magical_object` merges records by GUID match, so reused counters with same GUID stack cleanly but reused counters with different GUID produce two records claiming the same object reference, creating malformed entity cross-references. The earlier "reuse-counter bypasses Rule A" claim was validated only on single-item-type Moonstone probes where same-type merge sidestepped the corruption. Investigation stopped because the budget input field could be a save-file field OR a hardcoded chapter constant baked into the binary; the empirical chapter-correlated values (+2/+3/+7) are consistent with either, and disambiguation requires finding the field first.
  - **`tools/frida/probe_item_budget.js`** — standalone Frida probe (no `rw_lab.js` dependency) gating all per-call sync/resize logging behind an `inDeserialize` flag set on entry to `serde_hero_controller_persistent_data`. Quiet by default; `armBudgetTrace({verbose:true})` enables verbose mode. Wild-pointer detection removed (Frida's `Memory.readU8` over-flags valid heap addresses → false positives on every load).
  - **`tools/frida/rw_lab.js`** — accumulated multi-session additions: boss-timer trigger primitives, items-budget tracing, MP shard helpers (`shardsHc`, `shardsStatus`, `gainShards`), force-seed harness primitives. New self-contained blocks for each subsystem; conventions documented at the top of the file (block isolation, byte-patch save-and-restore, labeled log lines).

MAINT-32: Locator-section rollout + convention extension (2026-05-05)
  - **Convention extended.** `rw/docs/README.md` §"Locating <thing>" rewritten to describe two flavors: byte-stream (the existing one, exemplified by `talent-records.md` §"Picks-block locator (generalized)") and RE-side (new, exemplified by `chapter-map-and-boss-spawn-architecture.md` §"Locating these symbols on a new build"). The convention's purpose stated: each finding that documents critical RVAs / struct offsets / byte structures carries a procedural re-anchor section so symbols can be re-derived on a new exe build without prior context. Rationale also covers AI-driven re-derivation: the locator section is effectively an agent prompt — strings/RTTI/hashes/byte-patterns map 1:1 to Ghidra MCP tool calls.
  - **Rollout across 16 findings, 4 tiers.** Tier A (tool-anchoring): `chapter-boss-portal-trigger.md`, `save-subsystem.md`, `frida-pipeline-hardware-breakpoint.md` — Frida lab depends on these RVAs. `save-subsystem.md` got a 5-tier breakdown (RTTI / function strings / globals / struct offsets / vtable slots) covering ~170 RVAs. Tier B (confirmed RE-side): `save-edit-pipeline.md`, `save-flow-diagrams.md`, `save-silencer-mechanism.md`, `rng-behavior.md`. Tier C (in-progress RE-side): `multiplayer-host-authority.md`, `items-add-primitive-cap.md`, `items-budget-rule-a-investigation.md`, `ghidra-rule-c-investigation.md`, `talent-picker-weighting.md`, `starting-talent-bias.md` — tighter sections since these may evolve. Tier D (save-format byte-stream): `save-binary-format.md` (foundational magic bytes + GUID + CRC framing), `save-catalog-flag-bytes.md`, `save-account-binding.md`, `held-dream-shards.md` (both flavors). `talent-records.md` already had the canonical "Picks-block locator (generalized)" — no change needed; `magical-objects.md` already had `## Locating items` — verified adequate.
  - **Audit + spot-check.** `.ai/scratch/locator-audit-20260505.md` triages every active finding by RVA / offset / pattern density and assigns rollout tier. `.ai/scratch/locator-validation-20260505.md` documents 4 cold-execution validation tests: byte-pattern (`01 d9 d8 17` → BossTimer pair), single-string (`"Boss time start"` → registration site), error-message strings (encyclopedia register/unregister), and chapter-map-name strings. **All four tests passed** — anchor strategies returned predicted symbols every time without external context. Caveats documented: struct-offset stability across major engine version bumps is empirical-only; RTTI stability assumes future builds don't strip symbols; save-format anchors assume cipher and magic bytes don't rotate.

BREAKTHROUGH-5: Map-boss spawn architecture + encyclopedia primitive (2026-05-05)
  - **`rw/findings/chapter-map-and-boss-spawn-architecture.md` (in-progress).** End-to-end Ghidra dig of how chapter map-bosses (game's term: `Map_Boss_Spawner_<Chapter>`) load, spawn, and trigger. Pipeline mapped: `BossTimer_update` fires named event `0x17d8d901` → entity-component subscriber on `Map_Boss_Spawner_<Chapter>` activates the shared `Map_Boss_Spawner_Cinematic_Awakening` entity (the cinematic) → CineBlocs inside execute in sequence (fade-in load cover → `Sc_RequestSyncLoadData` for the boss bundle → `oCActivateEntityCpntCineBloc` activates the spawner → animations + audio → `oCEntityNamedEventSenderCineBloc` fires downstream events → fade-out). Cinematic playback IS the load cover — engine sync-loads boss-arena assets during cinematic duration; player perceives the cinematic, engine streams.
  - **The four chapter maps** are name-bound by static table at `g_chapter_map_name_to_hash_table @ 0x140eea480`: `Dark_Hills`, `Storm_Island`, `Avalon`, `Baba_Yaga_Map` (parallel stat hashes at `g_chapter_map_stat_hash_array @ 0x140eea740`). Each chapter has its own boss-arena `.level.ot` tile (`Tiles!40x40_Boss_Shrine_WhiteLady`, `_Jinn_Boss_Shrine_Arena`, `_Witch_Boss_Shrine`, distinct Outside/Interior arena for Baba Yaga) and its own `Map_Boss_Spawner_<Chapter>.entity.ot` prefab. The awakening cinematic is shared across all chapters; per-chapter variation is just the spawner's bound prefab reference.
  - **Encyclopedia primitive — the master key.** `oCEntitySettingsEncyclopediaSceneContext` is a scene-context that holds a Swiss-Tables hashmap (Abseil-style) of every entity-settings prefab loaded in the current session, keyed by name string. Layout decoded: control-bytes ptr at `+0x28`, entries array at `+0x30` (24-byte stride), count at `+0x38`, capacity-mask at `+0x40`; each entry's value (the `oCEntitySettingsResource*`) at `+0x10`. `EntitySettingsEncyclopedia_registerByName @ 0x1406eb7e0` and `_unregisterByName @ 0x1406ebfb0` use `hashmap_find_string_keyed_node @ 0x1406a0680` keyed by `fnv_string_hash_64 @ 0x140507510` (FNV mix constant `0xde5fb9d2630458e9`). Reachable via `scene_manager_find_context_by_type(scene_mgr, &type_tester_struct)` where the type-tester struct is `(_oCTSameTypeTester<oCEntitySettingsEncyclopediaSceneContext>::vftable, g_typeId_oCEntitySettingsEncyclopediaSceneContext @ 0x141447c18)`. **Implication:** Frida-callable `findEntitySettings(name) → oCEntitySettingsResource*` resolves any prefab in the game by name string — `"Map_Boss_Spawner_Storm_Island"`, `"Baba_Yaga_Boss"`, `"Map_Boss_Spawner_Cinematic_Awakening"`, anything. Generalizes well beyond bosses (40 separate `oCTLibrary<T>` catalogs in the binary — heroes, enemies, items, melodies, maps, tiles, rewards, dream shards).
  - **Engine reflection surface.** `oIEntityCpnt` base (parent of selectors, cinematics, spawners) exposes hash-keyed reflection at `vtable[11] = getMemberByHash` (data field accessor) and `vtable[19] = getMethodHandleByHash` (callable method handle return). Sc_* script-binding family decoded with method IDs and impl RVAs: `Sc_FindGo` (linear name search in level), `Sc_FindObject` (larger registry, host TBD), `Sc_GetLevel` (entity back-pointer at `+0x28`), `Sc_RequestSyncLoadData` (refcounted bundle load — `++[this+0x124]`), `Sc_Spawn` (per-entity instantiation), plus UI primitives `Sc_AddLabel`, `Sc_AddPicture`, etc.
  - **Universal bound-prefab pattern.** `oIEntity_resolveBoundPrefab_byIndex @ 0x140314e20` — every parent entity has `[+0x8C0]` settings-entries array (count at `[+0x8C8]`); each entry has paths at `+0x1c0`/`+0x1d0`, optional loader-class ref at `+0x1e0`, resolution flag at `+0x1e8`, **bound resource handle at `+0x1f0`**. When unresolved, the resolver walks `g_global_type_registry_root @ 0x141446f38` (loader catalog) for class id `0x53b64d` and dispatches load-by-name. Universal across HeroSpawner, Map_Boss_Spawner, and any other entity that holds child-prefab references.
  - **Spawner inheritance.** Confirmed via `oCDtEntityCpntHeroSpawner_ctor @ 0x1402ccf20`: HeroSpawner has multiple inheritance with `oCEntitySpawner::vftable` as a sub-object at `[+0x68]`. `oCEntitySpawner` is the doubly-linked-list manager for spawned entities (`+0x18` count, `+0x20`/`+0x28` head/tail). The bound prefab handle lives in a sibling component, not in `oCEntitySpawner` itself. `oCDtEntityCpntHeroSpawner_spawnHero @ 0x1402cd370` reveals a 4-slot prefab array at `this->[+0x10]+0x108` (0x50-byte stride) and the actual spawn call dispatched as `(this+0x68).vtable[+0x18](this+0x68, resolved_uuid, &spawnData)` which is `oCEntitySpawner_addSpawnedEntity @ 0x1406db390`.
  - **30 Ghidra function renames + 4 data renames + 2 plate comments** committed in the Ravensmith Ghidra project (full annotation log in finding's "Annotations applied this session" section). Notable: `Sc_FindGo_glue`, `oCGameLevel_findGoByName`, `oIEntity_Sc_GetLevel_impl`, `Sc_FindObject_impl`, `oIEntitySelectorToSpawn_onBindToParent`, `oIEntityCpnt_onAttach_commitToParent`, `oIEntityCpnt_persistenceAttach`/`Detach`, `oIEntityCpnt_getMemberByHash_baseImpl`, `oIEntityCpnt_getMethodHandleByHash_baseImpl`, `oCDtEntityCpntHeroSpawner_ctor`/`_spawnHero`/`_persistenceAttach`/`_persistenceDetach`/`_broadcastToTypedChildren_at_0x88`, `oCEntitySpawner_addSpawnedEntity`/`removeSpawnedEntity`, `oCSpawnablePool_allocateNode`, `oIEntity_resolveBoundPrefab_byIndex`, `EntitySettingsEncyclopedia_registerByName`/`unregisterByName`, `hashmap_find_string_keyed_node`/`insert_string_keyed_node`, `fnv_string_hash_64`, `EntitySettings_onLoad_registerToEncyclopedia`, `oCEntitySettingsResource_onLoad_registerToParent`. Data renames: `g_chapter_map_name_to_hash_table @ 0x140eea480`, `g_chapter_map_stat_hash_array @ 0x140eea740`, `g_global_type_registry_root @ 0x141446f38`, `g_typeId_oCEntitySettingsEncyclopediaSceneContext @ 0x141447c18`. Plate comments at `0x140eea480` (entry layout + four chapter map names + parallel hash array + consumer) and `0x1402907e0` (cross-ref appended to existing `chapter_end_work` plate).
  - **Wishlist additions** (`rw/docs/wishlist.md`): boss-rush mod (live, Frida) — staying in chapter 1 fight all map bosses back-to-back; encyclopedia name-key dump tool — Swiss-Tables walk + register-function hook; universal entity-spawn API surface — beyond boss-rush, the foundation work enables a developer-console mod surface (spawn any enemy, give any item, apply any melody). Three remaining integration TODOs before boss-rush works: encyclopedia type-tester vtable static address, bound-prefab field offset on a live `Map_Boss_Spawner` instance, chapter-end suppression hook on non-final kills. Architecture is structurally complete; integration is mechanical from here.

BREAKTHROUGH-4: On-demand chapter-boss arrival via Frida — proof-save scarcity dead (2026-05-03)
  - **Mechanism cracked.** The chapter-end boss arrival is gated by a single float comparison in `BossTimer_update` at `image+0x1e9d50`: when `elapsed (this+0x12c) >= boss_time (this+0x144)`, the engine sets `is_boss_awaken=1` and fires named event `0x17d8d901` ("Boss time start" / "Triggered when boss awakens"). The named event's existing subscribers spawn the boss arena. No need to reverse the spawn pipeline — write `elapsed = boss_time` and the engine fires its own event, identical to natural progression. Discovery path: string search "Boss" / "Awaken" / "Cycle" → `oCDtBossTimerUiControllerEntityCpnt` and modifier-stat strings ("Half Cycle Count Before Boss Awakens Modifier" et al.) → modifier-stat hash registration block at `0x1401eaf20–0x1401eb63c` → byte-pattern xref of the registered hashes (`0x17de260c`, `0x17de260e`, `0x17d8d900`, `0x17d8d901`, `0x1cd7928b`) to find the firer (`BossTimer_update`) and stat-publisher (`BossTimer_publish_state_to_property_bag` at `0x1ea2b0`). Same dig pattern as the talent picker (string → registry → hash xrefs).
  - **Frida harness landed** (`tools/frida/rw_lab.js`). New self-contained block adds `Interceptor.attach` on `BossTimer_update` that captures the live BossTimer instance pointer on every tick, plus three REPL commands: `forceBossSpawn()` (write `elapsed = boss_time`, the trigger), `bossTimerStatus()` (dump runtime state — elapsed, boss_time, day/night durations, all gating booleans), `bossTimerSetElapsed(seconds)` (arbitrary write for warning/overtime threshold testing). Field map and constants at top of file; ready-line updated; new BOSS-TIMER TRIGGER documentation block in the header. Conventions match the existing harness sections (force, pickerCount, probeForBossKillSave).
  - **End-to-end verified in-game.** Chapter-3 proof swap → started fresh chapter → at T+43s `bossTimerStatus()` reported `elapsed=3.05 boss_time=1260 arrivalEnabled=6 isAwaken=0` (clean, ticking). `forceBossSpawn()` issued; engine fired all three threshold events (overtime/warning/awakening — all crossed simultaneously when elapsed jumped 3.05 → 1260) and spawned the boss arena identically to natural progression. Boss kill triggered the normal chapter-end save dialog → new proof save written. **Total time elapsed for an end-of-chapter test: seconds, not minutes.**
  - **Ghidra annotations on the spot.** Three renames: `FUN_1401e9d50` → `BossTimer_update`, `FUN_1401ea2b0` → `BossTimer_publish_state_to_property_bag`, `FUN_1401eab90` → `BossTimer_register_stats_and_events`. Two plate comments: BossTimer_update documenting the threshold-crossing fire sites and the Frida one-liner trigger recipe; publish_state_to_property_bag documenting the full property-bag hash → field map.
  - **Finding promoted to confirmed.** `rw/findings/chapter-boss-portal-trigger.md` carries `Status: confirmed`, the verification result with the actual `bossTimerStatus()` capture, and the field map + Frida harness recipe. Wishlist's "On-demand chapter-boss portal trigger" entry removed (was added earlier the same session and completed end-to-end before the day was out).
  - **Impact.** CLAUDE.md "Saves are only generated at chapter-boss kills" gotcha is no longer a 20-minute-per-iteration tax. Save-edit experiments that depend on a fresh proof save (mint/edit → play → boss kill → save → re-inspect round-trip) collapse from ~20 min of focused play to ~2 minutes of "start chapter, run forceBossSpawn(), kill boss, save." Lifts the "treat proof saves as scarce" rule directly.

MAINT-30: Talent / item picker rarity-bias dig + wishlist hygiene (2026-05-03)
  - **`rw/findings/starting-talent-bias.md` (in-progress).** Investigated the community claim that starting talents at elevated rarity bias subsequent talent / item / shop picks toward the "direction of the special starting talent." Verdict: **identity / category bias NOT supported**, **cross-system rarity carry NOT supported**. Talent picker selection is uniform-random Fisher-Yates over the per-class pool; tier roll is identity-blind. Items picker (`MagicalObjectController_roll_proposed_objects` at `0x2d4740`, renamed from `FUN_1402d4740`) reads only its own two modifier hashes (`0x1709d229` "MO better quality chance", `0x1893b9e4` "Extra MO Choice") — no SkillController state, no slot-tier mirror, no starting-talent record. Hash-byte audit confirmed every modifier hash in either pipeline is read by exactly one of the two systems; no cross-leak. Real (smaller) effect documented: per-slot rarity stickiness in the talent picker only — a Legendary starting talent in slot N keeps stamping Legendary on subsequent picks for that slot (per-slot, rarity-only, doesn't affect items). Renames: `FUN_1402d4740` → `MagicalObjectController_roll_proposed_objects`, `FUN_1402d5230` → `mo_roll_rarity_bucket_weighted`, `FUN_1402d4200` → `MagicalObjectController_repropose_objects`. Plate comments on the items picker and bucket-roll documenting the no-talent-state assertion.
  - **Wishlist cleanup.** Removed `Slot 6–10 controller GUID → talent-name cross-reference` (already conquered by MAJOR-1 picks-block universal locator + the controller→GUID lookup table in `tools/rerw-src/data/heroes/geppetto.yaml` and `rw/dumps/geppetto/talent-record-decoded.txt`; verified by user request to check git+ledger).

MAINT-29: Post-mint `clear-picks` locator fix + Romeo Mint promoted (2026-05-03)
  - **Bug fix in `find_picks_count`** (`tools/rerw-src/lib/talent_edit.py`). The picks-block locator validated the trailer pattern `[8 zero][float][8 zero]` followed by a `100.0 < f_val < 5000.0` "play-time range" sanity gate. `rerw mint savefile` zeroes per-run timing stats including that float, so post-mint saves had `f_val == 0.0` and the gate rejected them — `rerw write savefile clear-picks` failed on any mint output with `Could not locate picks-count u32`. Fix: accept `f_val == 0.0` alongside the populated-run range. The 8-zero/8-zero sandwich plus the close marker is already a strong structural signature; the float-range check was paranoid overhead. Regression caught by attempting an end-to-end mint chain (mint → hero → level → all-talent-rarities → clear-picks); pre-fix forced fallback to the legacy `rw/scripts/zero_picks_count_v2.py` which uses the chapter-2 sentinel and is unaffected.
  - **Romeo Mint promoted** to `rw/saves/mints/geppetto/chapter2/laser-lenses_1/romeo-pickscount0-all10-legendary/` (mint convention: source-chapter path, `info.md` required next to `Profile_1.ob`). Build chain: `rerw mint savefile` → `hero --key romeo` → `level 14` → `all-talent-rarities legendary` → `clear-picks`. End-to-end CLI; the legacy `zero_picks_count_v2.py` workaround used during recipe discovery is now obsoleted by the bug fix above. `info.md` covers provenance, reproduction recipe with order-dependency commentary, in-game verification, and chapter-3/epilogue generalization (the chain is fully chapter-agnostic — only `--source` changes). Closes the wishlist's "Romeo Mint promotion" item; bullet removed from `rw/docs/wishlist.md`.

PRP-6: rw/ documentation overhaul (2026-05-03)
  - **Findings consolidated.** `rw/triage/` and `rw/key-findings/` (incl. `archive/`) merged into a single flat `rw/findings/`. Each doc carries a canonical `**Status:** in-progress|confirmed|archived` line (regex-strict; manifest grep depends on it). 30 docs migrated; 9 with missing Sources headers backfilled (most with the `pre-policy — written before the Sources header was mandatory` fallback phrasing). Promotion is now a one-line edit, no `git mv`. New `/findings-status` slash command regenerates `rw/findings/STATUS.md` from a grep of the canonical Status lines (no LLM-semantic reasoning, no hooks/CI). Initial STATUS.md: 7 in-progress / 15 confirmed / 9 archived. New `rw/findings/README.md` indexes the directory and points at `rw-findings-report` (renamed from `rw-triage-report`) for new doc creation.
  - **Workflow tree built.** New `rw/docs/workflow/` with five chapters following one template (What this is for / Concepts / Recipes / Gotchas / Pointers): `save-editing.md` (lifecycle, taxonomy, CLI primitives, gotchas), `frida.md` (technique + REPL reference + memory analysis full-process snap/diff folded in from the deleted `tools/mem-snapshot.md`), `ghidra.md` (annotation-on-the-spot discipline lifted from CLAUDE.md), `windbg.md` (live-debug + boss-spawn anti-debug gotcha), `cross-cutting.md` (tag=0x12 run-state record format extracted from talent-records + magical-objects). README.md indexes the chapters and codifies status-field/manifest/archive process rules.
  - **Per-save notes unified to `info.md` template.** Three-section template (Provenance / Reproduction recipe / Verified in-game) — no Status field (asymmetric with findings; freshness lives in directory taxonomy + git history), no "Why this works" / Mechanism pointers (would rot when findings get renamed/split/merged). 8 `breakthrough.md` files extracted to `info.md` and deleted; 1 `PROVENANCE.md` renamed/reformatted; 4 pre-template `info.md` files conformed; the `laser-lenses_1` parent `info.md` split-folded (runbook prose extracted to `workflow/frida.md` §Memory analysis; per-save provenance for the 5 sibling `level<N>/` directories preserved).
  - **CLAUDE.md progressive-disclosed.** Save-edit operational rules (chapter-boss-kill, swap, error modal, lab base, lab naming, save-file taxonomy) collapsed to 1-2 line directives that point at `rw/docs/workflow/save-editing.md` for narrative. Ghidra annotation-on-the-spot rule lifted to `workflow/ghidra.md`; CLAUDE.md carries a 1-line directive. Steam Cloud section shrunk to the canonical "assume disabled / mid-session swap clobber still applies" form. No `@import` syntax used (intentional — duplication is the point; agent's load stays scannable).
  - **Folds and deletes.** `rw/docs/playbook.md`, `rw/docs/save-edit-capabilities.md` (reconcile-then-fold to drop CLAUDE.md duplicates), `rw/docs/tools/mem-snapshot.md`, `tools/frida/HANDOFF.md` deleted after content folded. `tools/frida/HANDOFF.md` → new finding `rw/findings/frida-pipeline-hardware-breakpoint.md` (`Status: in-progress`; the hardware-breakpoint task on `data_source+0x1958` is the next concrete step). Cross-cutting extraction shrinks the duplicated tag=0x12 picks-block sections in `talent-records.md` and `magical-objects.md` to one-line pointers at `workflow/cross-cutting.md`. New `## Locating items` subsection added to `magical-objects.md` per the "Locating <thing>" convention codified in `rw/docs/README.md` §Doc conventions. Date-strip rename `save-edit-pipeline-2026-04-30.md` → `save-edit-pipeline.md` (six refs swept). Six findings flipped to `Status: archived` with banners (decoder-work, mint-hardcoded-offsets, save-event-not-firing, level-runtime-address, live-state-mirror-cascade, geppetto-save-analysis) — files stay at the same path so existing cross-references resolve. `tools/rerw.md` "items not wired" stale paragraph replaced with a pointer to `workflow/save-editing.md`.
  - **Sweeps.** Two-pass repo sweep rewriting `rw/triage/` and `rw/key-findings/` to `rw/findings/` — markdown surface (rw, tools, .claude, CLAUDE.md, AGENTS.md, TASKS.md) and source/data/CLI-help surface (`tools/rerw-src/lib/*.py`, `tools/rerw-src/commands/*.py`, `tools/rerw-src/data/*.yaml`, `tools/rerw-src/tests/unit/*.py`, `tools/frida/save_now.js`, `rw/scripts/inspect_picks_block.py`). Skill rename `rw-triage-report` → `rw-findings-report` propagated; template now writes to `rw/findings/<slug>.md` and seeds `**Status:** in-progress`. Backlinks (`[← Back to <parent>](README.md)`) added to all non-README docs under `rw/docs/workflow/`, `rw/findings/` (incl. archived), and `rw/ref/`. New `rw/ref/README.md` covers the tracked-snapshot tree files and screenshots dir. `rw/docs/README.md` table updated (workflow / findings / ref rows added; playbook row dropped); new `## Doc conventions` section codifies backlinks, README-as-TOC, no See-also footer, host-paths rule, and the "Locating <thing>" subsection convention.
  - **Audit cleanup.** Original `.ai/scratch/ravensmith-docs-audit.md` and `*-medium.md` deleted (their job is done; PRP-6 is the durable artifact). The PRP-6-specific `.ai/scratch/docs-consolidation-handoff-20260503.md` deleted. 18 `rw-context-handoff-*.md` and 1 `ghidra-mcp-context-handoff-*.md` moved from flat `.ai/scratch/` to `.ai/scratch/context-handoff/`; `.claude/commands/rw-context-handoff.md` updated to write into the subdir.
  - **Final validation gates pass.** Every finding has exactly one `^\*\*Status:\*\* (in-progress|confirmed|archived)$` line and a Sources header. Zero remaining `rw/triage/` or `rw/key-findings/` refs in tracked surfaces (sole holdouts are `rw/dumps/` files, gitignored). Zero `breakthrough.md` / `PROVENANCE.md` refs. STATUS.md regenerated; `tools/frida/HANDOFF.md` no longer exists.

MAINT-28: Codify mint as first-class save artifact type + wishlist parking lot (2026-05-03)
  - **Mint promoted to first-class artifact type alongside lab/golden.** `rw/docs/playbook.md` rewritten: directory tree adds `saves/mints/<hero>/<source-chapter>/<run-id>/<mod-id>/{Profile_1.ob, info.md}`; What-Goes-Where table gains a "Verified mints" row; promotion flows 4 → 5 (lab→golden split into "non-mint chain" and "lab→mints" for mint-chain promotions). Mints are NOT a refinement step beyond golden — the distinction is whether the build chain includes `rerw mint savefile`. Mint definition pinned precisely: the mint command zeros held currencies (shards / stars / feathers / keys), per-run stats (damage / time / shards earned / feathers consumed), end-of-run score-page (score floats, chapter-progression banner, ActivityScore records), level→1, xp→0, chapter→1; subsequent chain steps may override specific fields. Mint dirs MUST include `info.md` next to `Profile_1.ob` (recipe required, not optional).
  - **Save directory conventions rewritten.** Old "Save Edit Directory Conventions" section replaced with "Save Directory Conventions" covering all three artifact types. New subsections: Definitions / Path conventions / Examples / Hygiene. Path-convention precision: golden's `<chapter>` is the POST-EDIT chapter; mint's `<source-chapter>` is the SOURCE proof's chapter (mints output chapter 1 by definition, so source-chapter is the more informative axis); mint mod-ids carry no `__from-...` suffix (source is encoded by the path itself). Hygiene rule about deleting failed labs preserved.
  - **Terminology cross-link.** `rw/docs/terminology/README.md` adds "Save artifact types" section with quick-reference definitions and a pointer to `playbook.md` for the full rules.
  - **Wishlist parking lot.** New `rw/docs/wishlist.md` for future-work ideas that surface mid-session and shouldn't drift. Three sections: CLI surface (per-stat individual setters, mint flavor flags, mint chain shortcut), Investigations/digs (Continue-vs-new-game gate, master chapter seed, item/chest/Sandman picker harness, save serializer hook, GUID hash-tail reversal, source-of-truth pin identity, remaining held resources, controller-GUID lookups, per-hero verification of talent format), Documentation/housekeeping (Romeo Mint promotion, `add-level-record` shelving decision, skill-controllers YAML schema mismatch, `rerw experimental` cleanup, pre-existing test-suite drift). Items move to `.ai/TASKS.md` with a code when picked up; deleted from wishlist on pickup. Indexed in `rw/docs/README.md`.

MAJOR-1: Picks-block universal locator + tier→rarity rename + Romeo all-10-empty golden + Frida picker-count patch (2026-05-03)
  - **Picks-block locator generalized across all chapters.** The chapter-2 `00 00 00 00 05 00 00 00` sentinel turned out to be coincidental (collapsed set-bonus tracker; not a structural anchor) and never matched chapter-3 / epilogue saves. Decoded the actual layout via `rw/scripts/inspect_picks_block.py`: `[u32 N count][N × 16-byte GUIDs]` followed by a fixed trailer `[8 zero][float timing][8 zero][22 22 bb aa close]`. Replaced the sentinel search in `lib/talent_edit.py` with a count-agnostic walker that backs up from the close marker, validates the trailer, and back-fits N in 1..15. New primitives `find_picks_count(data, record_off) -> (count_off, count)` and `clear_picks(data, record_guid_15)`. Verified across Geppetto chapter-2 (N=5), chapter-3 (N=8), and epilogue (N=10) proofs. `data/save-fields.yaml` and `lib/save_fields.py` simplified — the `talent_picks` field type no longer requires `sentinel` / `slot_count` (now derived at runtime). `commands/read_savefile.py` and `commands/write_savefile.py` adopted the new locator (deprecated `--talent-slot` range bumped 1..5 → 1..10). Lab `talent-slot6-to-twin-dummies__from-epilogue` (built via `rw/scripts/lab_build_slot6_to_twin_dummies.py`) verifies slot-6 writes work on count=10 saves.
  - **Tier → rarity terminology rename.** New `rw/docs/terminology/README.md` codifies project-wide terms (talent vs talent-pool vs picker-pool; item vs catalog; seed disambiguation between TLS gameplay seed, master chapter seed, and Forced Seed UInt; 1-based user / 0-based code translation rule). `CLAUDE.md` "Terminology" rule points at it; `rw/docs/README.md` indexes it. `lib/talent_edit.py` renamed throughout: `TIER_NAMES`→`RARITY_NAMES`, `write_tier`→`write_rarity`, `parse_tier`→`parse_rarity` (now strict-lowercase: only `common`/`rare`/`epic`/`legendary` accepted; numeric and aliases removed), `write_all_slot_tiers`→`write_all_slot_rarities`, `write_all_tier_bytes`→`write_all_rarity_bytes`. `read_savefile` output relabeled accordingly.
  - **CLI surface updates.**
    - `rerw write savefile talent`: slot range bumped 1..5 → 1..10 (now usable on chapter-3 / epilogue saves). `--tier` flag renamed `--rarity` (`-r` short, was `-t`); metavar tightened to `<common|rare|epic|legendary>` (lowercase only).
    - `rerw write savefile tier` → `rerw write savefile talent-rarity`. Slot range 1..4 → 1..10 with runtime rejection of slot 5 (the ult). `--rarity` flag and lowercase-only metavar.
    - `rerw write savefile all-talent-rarities <rarity>`: argument metavar tightened to `<common|rare|epic|legendary>` (numeric and aliases removed). Output text relabeled rarity instead of tier.
    - New `rerw write savefile clear-picks`: zeros the picks-count u32 and deletes the N×16-byte GUID block. No edit-chain ordering footgun for new code paths (the universal locator does not depend on the chapter-2 sentinel), but legacy callers that still rely on the sentinel must run before this.
    - Deprecated parent flag `--tier` on `write savefile` renamed `--rarity`; deprecation warning text updated to point at `talent-rarity` subcommand.
  - **All-rarities ult-slot skip changed value-based → index-based.** `write_all_slot_rarities()` now skips slot index 4 (the ult) instead of "any entry currently equal to 4." Extends Legendary coverage to user slots 6-10 (previously left at the engine's `value=4` uninitialized sentinel for slots the player hadn't reached). Verified by lab `romeo-ch1-level14-pickscount0-all10-legendary__from-romeo-golden` (built via `rw/scripts/lab_build_romeo_all10_legendary.py`). The `skip_ult_marker=False` optional parameter on both bulk-set primitives was removed (skip is now hardcoded to the ult-slot index for slot-array writes and to ult-marker-value for tag=0x10 byte writes).
  - **Tests updated** (`tests/unit/test_lib_talent_edit.py`): all tier→rarity renames propagated; new strict-lowercase `parse_rarity` tests (accepts only `common`/`rare`/`epic`/`legendary`; rejects numeric input, uppercase/mixed-case, unknown names); `write_all_slot_rarities` test updated to assert index-based skip (modified count = 9, slot index 4 preserved); removed two `skip_ult_marker=False` tests since that parameter is gone.
  - **Romeo all-10-empty Legendary golden.** New golden `rw/saves/edits/golden/romeo-ch1-level14-pickscount0-rarities-legendary__from-laser-lenses_1-proof/` with `breakthrough.md` recipe. Recipe driver `rw/scripts/zero_picks_count_v2.py` moved from gitignored `rw/dumps/` to committed `rw/scripts/` per the new dumps-vs-scripts rule (the script is referenced from the breakthrough.md recipe, so it must live in `rw/scripts/`).
  - **Frida live-patch lab.** Renamed `tools/frida/force_seed.js` → `tools/frida/rw_lab.js` (now a hub script for runtime patches + diagnostics, with conventions documented in the header). Added `pickerCount(n)` / `unpatchPickerCount()` REPL commands that patch both immediates computing the talent-picker offered count (`image+0x39c4cd` LEA imm `0x02`, `image+0x39c4e4` ADD imm `0x04`) so every slot offers `iVar18 + n` talents. Saves originals on first apply; restores cleanly. Backed by a Ghidra dig (working notes in `rw/dumps/talent_weighting_ghidra_findings.md`) identifying the picker pipeline (`SkillController_roll_proposed_skills`, `talent_roll_tier_weighted`, `talent_stamp_tier`) and the run-wide gameplay-modifier hashes (`0x1709d22b` "Skill better quality chance", `0x1bfb2a14` "Reset skill better quality chance", `0x1871c2fa` "Rare Skill Chance Modifier", `0x1a7a3166` "Extra skill choice", `0x1aa49d8d` "Free Reroll"). Updated `tools/frida/README.md`, `tools/frida/HANDOFF.md`, `rw/findings/rng-behavior.md`, and `rw/findings/random-seed-system.md` to reference the new filename.
  - **Process / ops codification.**
    - `CLAUDE.md`: added "Scripts: dumps vs scripts" rule (any script referenced from a committed doc must move from `rw/dumps/` to `rw/scripts/`); added "Terminology" rule; rewrote "Save-file swap" section to be terser and drop Steam-Cloud language; added new "Steam Cloud sync" section asserting cloud sync is OFF on this machine (no warnings, no factoring); clarified `UV_CACHE_DIR` is project-wide via `settings.json` rather than a per-command prefix.
    - `.claude/settings.json`: added `UV_CACHE_DIR=/tmp/uv-cache` env var so `uv run rerw` works in the agent sandbox without per-call prefixes.
    - `rw/docs/playbook.md`: added 4th promotion path (`rw/scripts/` → `rerw` CLI) and new "CLI Promotion" section codifying the lab-verify-in-game-before-CLI-change rule.

MAINT-23: `rerw write savefile item swap|add|remove` CLI subcommands (2026-05-01)
  - New `tools/rerw-src/lib/item_edit.py` exposes `find_run_state`, `parse_records`, `swap_item`, `add_item`, `remove_item`, plus `ItemEditError`. Pure-logic primitives operating on the `tag=0x1a` records inside the run-state record (same `tag=0x12` record that holds talent picks). Constants for `ITEMS_COUNT_OFFSET=0x5d` and `RECORDS_ARRAY_OFFSET=0x61` (relative to the run-state tag-byte offset) verified empirically across chapter-2 / chapter-3 / epilogue saves.
  - New nested `rerw write savefile item` group with three subcommands. `swap --slot N --key X` replaces a slot's runtime GUID (constant size). `add --key X [--reuse-counter-from-slot N]` appends a record (file +32 bytes); the optional reuse-counter flag bypasses the engine's Rule A fresh-reference cap. `remove --slot N` drops a record (file -32 bytes). All resolve item keys strictly via `lib.game_registry.magical_items().lookup(key)` — display names and aliases rejected.
  - Tests at `tools/rerw-src/tests/unit/test_lib_item_edit.py` (14 cases, all passing): find / parse / error paths plus byte-equality validation against three verified-working reference artifacts — `item-add-fill-moonstone-stack-5of5` golden (ADD), `item-vorpal-blade-to-baba-yagas-mortar` lab (SWAP), `item-remove-last-record` lab (REMOVE). Round-trip remove-then-add returns to source bytes verbatim. CLI smoke-tested end-to-end: every subcommand produces byte-identical output to its corresponding reference artifact (CRC `0x1BDCE948` for the SWAP matches the documented value in `magical-objects.md`).
  - Updated `rw/docs/save-edit-capabilities.md` to move all three item primitives from "proven save edits without a CLI" up to "CLI one-liners" with a footnote covering the engine ceilings (Rule A / B / C).
  - Updated `rw/findings/magical-objects.md` "Tooling" section to reflect that the lib + CLI are wired; supersedes the prior "not yet wired" status.
  - **In-game validation (post-commit, 2026-05-01):** all three primitives confirmed end-to-end via CLI-produced labs swapped into the live save. ADD lab `item-add-5of5-moonstones__from-laser-lenses_1__cli` loaded with 5× Moonstone, set bonus active, damage increased as expected; SWAP lab `item-swap-slot8-mortar-ghost__from-laser-lenses_1__cli` showed slot 8 as Water of Life (Mortar ghost reroute) with +25 vitality; REMOVE lab `item-remove-slot21__from-laser-lenses_1__cli` loaded with Philosopher's Stone gone, total items 14 → 13. Labs are gitignored per project convention; not promoted to golden (CLI byte-identical to existing reference artifacts so promotion adds no signal).

MAINT-22: `rerw write savefile hero` CLI subcommand (2026-05-01)
  - New `tools/rerw-src/lib/hero_edit.py` exposes `find_hero_record(data)` and `swap_hero(data, save_ref)` — pure-logic primitives that splice the length-prefixed `Heroes\<EngineName>.herodef.ot` reference. Custom `HeroEditError` for precondition failures (missing pattern, multiple matches, bad prefix length). Reuses `HERO_PATH_RE` from `lib/talent_edit.py`.
  - New `rerw write savefile hero --key <HeroKey>` subcommand. Resolves the strict key via `lib.game_registry.heroes().lookup(key)` to obtain `save_ref`; mutates raw bytes via `swap_hero`; recomputes CRC via `lib.save_edit.recompute_crc`. Same-length / shrink / grow all handled by `bytearray` slice assignment (file size shifts naturally).
  - Tests at `tools/rerw-src/tests/unit/test_lib_hero_edit.py` (9 cases, all passing): find/error paths plus byte-equality validation against all 4 verified-working goldens (`hero-swap-to-{carmilla,aladdin,snow_queen,red}`) covering the no-shift / -1 / +2 / -5 byte cases. Round-trip Geppetto -> Carmilla -> Geppetto returns to source bytes verbatim. CLI smoke-tested end-to-end against the same 4 goldens — every output is byte-identical to the verified golden.
  - Updated `rw/docs/save-edit-capabilities.md`: hero swap moved from "proven save edits without a CLI" up to "CLI one-liners" with a footnote covering name-length shifts and on-load slot behavior.

MAINT-21: Sync triage docs to BREAKTHROUGH-2 (2026-05-01)
  - `rw/findings/geppetto-save-analysis.md`: moved In-Run Dream Shards, Held Raven Feathers, and Held Nightmare Keys from "Still unresolved" to "Conquered" with bytefield offsets and CLI refs. Expanded "Item Counts" to note the three held-inventory currencies live in three DIFFERENT records (HeroIngredient vec at HC+0x21 for keys, CRP+0x15D for feathers, HC+0x1D for shards) and that magical-objects add/swap/remove is mechanism-decoded with one golden but no CLI yet.
  - `rw/findings/save-mint-status.md`: item (1) Open follow-ups updated to flag shards + feathers RESOLVED with their actual byte locations, narrowing the open hunt to wood / bean / other ingredient-type held resources. Item (2) main bullet updated similarly.
  - `rw/findings/random-seed-system.md`: status note rewritten — the held-inventory hunt is mostly resolved, so this item moves up the queue once wood/bean finish or are deferred.

MAINT-20: Save-edit capabilities reference doc (2026-05-01)
  - New `rw/docs/save-edit-capabilities.md` cataloguing every save modification the project can perform today, in three categories: CLI one-liners (`rerw write savefile <field>`, `mint`, `swap`), proven edits without a CLI (hero swap, magical-object add/swap/remove, zero-keys insertion — each cross-linked to the key-finding doc and any verified-working golden), and genuinely unmapped fields (random seed, HC+0x11/+0x15 unidentified damage floats, held wood/bean, lifetime cumulative stats).
  - Also documents the run-state cleanup that mint applies automatically (score floats, ActivityScore records, chapter-progression banner, playtime, dream-shards earned/spent, held inventory, level/XP).
  - Real-world gotchas section folds in the chapter-boss-kill-only save constraint, Steam Cloud + swap-while-running clobber hazards, and the false-negative save-load error modal.
  - Cross-references the canonical pipeline (`save-edit-pipeline-2026-04-30.md`), held-dream-shards reference, hero-swaps recipe, magical-objects format, and the rerw-src libraries (setters, save_mint, hc_walker, game_registry).

MAINT-19: `rw-context-handoff` slash command — add delta mode (2026-05-01)
  - Same-day re-invocations now produce a short delta document (Prior handoff / Deltas since prior / Next steps) referencing the most recent prior handoff for unchanged context, rather than re-emitting a full snapshot.
  - Mode selection: explicit `--full` overrides; otherwise if a `rw-context-handoff-<YYYYMMDD>-*.md` already exists for today, delta mode is used; otherwise full mode.
  - Full mode tightened: each section capped ~5 bullets unless inherently larger (paths), several "include only if applicable" sections (Tried-and-ruled-out / Open questions / Blockers) made conditional rather than always-on.
  - Prime directive block extracted as a shared header for both modes.
  - Output filename collision still handled with `-NN` suffix on same-second invocations.

MAINT-18: Align hero + magical-item YAMLs to strict-key registry schema (2026-05-01)
  - 12 hero YAMLs add `schema_version: "1.0.0"`, `registry_id: hero_talents`, and a `hero:` block with `key` + `engine_name`. Closes the data side of the strict-key migration shipped in PRP-4 (`lib/game_registry.py`).
  - `magical-items.yaml` adds `schema_version: "1.0.0"` and `registry_id: magical_items`. Header comment dropped the obsolete reference to fuzzy normalization (case/whitespace/dashes/underscores) since the resolver is now strict-key only.
  - Per-controller / per-item bodies are unchanged structurally; only the file-level metadata block is new.
  - The legacy `lib/skill_controllers.py` loader (used by deprecated `--talent-id` flag) still expects the pre-migration schema. Acceptable per the deprecation plan; not regressed.

BREAKTHROUGH-2: Held Dream Shards bytefield mapped + `write savefile shards` (2026-05-01)
  - Located held Dream Shards at `oCDtEntityCpntHeroControllerPersistentData` body+0x1D as float32 LE (byte-misaligned). Same offset across all chapters (front-anchored in the static prefix).
  - Verified authoritative direct-read: lab `held-shards-99__from-mint__from-chapter3-laser_lenses_1-proof` patches HC+0x1D from 0.0 → 99.0 on a mint golden where earned (HC+0x19) and spent (dynamic offset) are both 0.0; HUD shows 99 on load. Game does NOT recompute held = earned − spent.
  - `lib/hc_walker.py`: renamed `damage_float_3` → `dream_shards_earned`, `damage_float_4` → `held_dream_shards`. Comments tag the still-unidentified `damage_float_1`/`_2` at HC+0x11/+0x15.
  - `lib/setters.py`: added `set_held_dream_shards` (float32). `zero_per_run_damage` docstring updated to reflect the now-known meaning of the +0x19/+0x1D pair (it already zeros the whole 16-byte block; mint behavior unchanged).
  - `rerw write savefile shards <float>` subcommand wired through `_apply_setter_edit`. CLI smoke-tested: 0.0 → 50.0 round-trips through encode + parse.
  - New key-finding `rw/findings/held-dream-shards.md`: standalone bytefield reference with cross-checks for ch2/ch3 (held = earned − spent invariant holds in observed natural saves but is not enforced on load).
  - Updated `rw/findings/save-edit-pipeline-2026-04-30.md` HC body section + stat-source mapping + known-gaps to reflect the dream-shards mapping.
  - Promoted lab → golden: `rw/saves/edits/golden/geppetto/chapter1/held-shards-99__from-mint__from-chapter3-laser_lenses_1-proof/` with breakthrough.md.

PRP-4: Strict-key game registry + `game-assets inspect` + held-keys write (2026-05-01)
  - MAINT-15 absorbed ~30% of the original proposal (per-field write subcommand consolidation under `rerw write savefile`); this PRP delivers the still-valid remainder. Original proposal at `.ai/planning/prp/proposals/PRP-4_rerw-strict-key-writes-and-asset-inspection.md`.
  - New `lib/game_registry.py`: strict-key views over `heroes`, `hero_talents`, `magical_items` with schema validation (major-version match on `schema_version`, exact `registry_id` match). No aliases, no fuzzy, no display-name fallback.
  - New `rerw game-assets inspect heroes|talents|items` discovery commands. Default: NAME / KEY (+ DESCRIPTION for items/talents). `-n` omits description; `-v` adds developer fields (GUID, EFFECT/CONTROLLER, etc.).
  - Migrated `rerw write savefile talent --key` from fuzzy `resolve_talent_id` to strict `registry.hero_talents(hero).lookup(key)`. Display names and aliases now error; only canonical keys accepted.
  - Added `rerw write savefile keys <int>` for held Nightmare Keys count. Updates existing keys record's count u32; empty-vec insertion (creating a new record on a save without one) deferred.
  - `lib/setters.py` gains `set_held_keys`; mint zero-sequence calls it (no-op for empty-vec proofs, zeroes count u32 for saves with an existing record like test3-mint).
  - Parent-level deprecated flags on `write savefile` now carry `[DEPRECATED]` help labels pointing to their replacement subcommands.
  - Legacy `rerw write savefile --talent-id` (deprecated parent flag) still errors via the legacy loader's outdated YAML schema expectations — not regressed; tracked under YAML schema migration.
  - Implemented across two commits: `e929911` (registry + inspect + strict talent migration; originally labeled MAINT-16) and `f13ca09` (keys subcommand + deprecation labels; originally labeled MAINT-17). Consolidated under PRP-4 in TASKS.md; commit messages on the remote retain the MAINT labels.

MAINT-15: Refactor mint + write savefile CLI into per-field subcommands (2026-05-01)
  - Mint becomes a pure transformation; semantic flags removed (`--chapter`/`--stars`/`--level`).
  - New `lib/setters.py` exposes pure setter functions; mint and write savefile share the same library so their behavior can't drift.
  - `rerw write savefile` is a Click group with per-field subcommands: chapter, feathers, level, stars, xp, talent, tier. Single-value edits are positional (e.g. `feathers 6`); multi-value use named flags (`talent --slot 1 --key X`).
  - Legacy parent flags (`--chapter`/`--level`/`--talent-*`) preserved with a deprecation warning; will be removed in a future release.
  - `$RERW_SAVEFILE` env var supplies `--source` default for both mint and write savefile.
  - 1-based user-facing indices (chapter 1..4 where 4=epilogue; talent slot 1..5; tier slot 1..4).
  - Held Raven Feathers (CRP body+0x15D, identified earlier this session) is now zeroed by mint and editable via `write savefile feathers`.
  - Subcommands `hero`, `keys`, `shards` deferred (next iteration).

MAINT-14: Sync key-findings + triage docs to BREAKTHROUGH-1 (2026-05-01)
  - `save-silencer-mechanism.md`: noted AS-removal as current production fix, preserve-bodies marked superseded, compounding-blanks hypothesis marked VERIFIED.
  - `save-edit-pipeline-2026-04-30.md`: documented the chapter-progression banner u32 in CRP body; Known-gaps section updated to reflect resolved items.
  - `save-mint-status.md`: activity-icon carryover marked RESOLVED; chapter-2-only mint gate noted as removed.
  - `mint-hardcoded-offsets.md`: marked RESOLVED (option C implemented via `lib/hc_walker.py`).

BREAKTHROUGH-1: Mint carryover bug fixes; mint goes chapter-agnostic (2026-05-01)
  - Bug fix: chapter-N ActivityScore icons carrying into derived mints' score-details panel.
  - Bug fix: chapter-progression banner carrying source-proof run history into derived mints' end-screen.
  - Folded dynamic HC body walker into production (`lib/hc_walker.py`); lifted chapter-2-only mint gate.
  - Tests parametrized over discovered `rw/saves/proofs/**/Profile_1.ob` fixtures.
  - New chapter-1 golden `mint__from-chapter3-laser_lenses_1-proof/` (production-CLI output); two prior chapter-1 goldens marked SUPERSEDED.

EXPERIMENTAL-3: Save mint POC — zero every per-run stat we could find + Stars of Fate edit + chapter-1 round-trip (2026-04-30)
  - **Full per-run zero-out recipe (the mint)** — chapter-2 proof was zeroed across every per-run record we know: HeroController damage region (4 floats at body+0x11/+0x15/+0x19/+0x1d), HeroController body+0x35d (originally 990 — turned out to be dream-shards-collected, accidentally zeroed thinking it was a damage stat), `ActivityScore × 6` nested under `oCDtCurrentRunProfileData` replaced with synthesized 25-byte minimum bodies, `HeroScoreData` 28 score floats zeroed in place (cached snapshot), `oCDtCurrentRunProfileData` body+0xe5 (playtime float = 1409s = 23:29 → 0). **Only one per-run stat we couldn't zero**: HeroController body+0x25 ("Raven Feathers consumed" stat — score-page row stays stuck at the written value, mirror-cascade behavior, writing 0 doesn't take). Plus body+0x2d (unknown stat — same stuck behavior). Both deferred.
  - **Hero level + accumulated XP zeroed together** to keep the per-run "Level reached" stat from being inflated by leftover XP. `oCDtEntityCpntGroupLevelPersistentData` body+0x11 = 1 (in-run hero level via `rerw write savefile --level 1`); body+0x15 = 0 (accumulated XP, byte-misaligned u32, sat next to the level field). Empirically established the formula `effective_level = hero_level + XP / xp_threshold`: ch2 proof showed 5.54 (lvl=5, XP=2690), `--level 1` alone produced 7.73 on defeat (= 1 + ~6.73 from leftover XP), `--level 1` + XP=0 produced the correct ~5.x baseline. Per-hero lifetime sum that aggregates each run's effective level across runs still TBD — likely in `oCDtHeroProfileData`; deferred. Per-run input to the sum is now fully controllable.
  - **Stars of Fate live edit (the major player-facing hack)**: discovered live spendable Stars-of-Fate count at HeroController body+0x29 (u32, byte-misaligned). Verified spendable in-game with edited baseline=7. This is arguably the most important save-edit win to date — Stars of Fate are the talent-reroll consumable, so editable count = defeat RNG on talent picks.
  - **Promoted three goldens** (chained — each is a stage in the mint round-trip): `golden/geppetto/chapter2/laser-lenses_1/zero-scores-and-level1/` (the v4 zeroed chapter-2 golden, CRC `0xDA0EFBA5` — the zeroed-at-chapter-2-position state); `golden/geppetto/chapter1/zero-scores-stars7-ch1/` (the chapter-1 minted starting save, CRC `0xC14F2CBB` — built with v4 zero-out + Stars=7 + `rerw --chapter 0`); `golden/geppetto/chapter2/2for1-thru-with-inventory/` (the chapter-2 follow-on after playing the chapter-1 minted golden through to chapter-2 boss kill, with held inventory keys/feathers/bean/extra dream-shards). All three have `breakthrough.md` + relevant screenshots. The chapter-1→chapter-2 round-trip is the proof the mint pipeline actually works end-to-end.
  - **Three things still uncontrolled** (documented as known limitations in the goldens' breakthrough docs and consolidated in new triage `rw/findings/save-mint-status.md`): (1) held-inventory byte location unmapped despite empirical persistence (keys carry through save → restart cycles per user test, but no record we diffed shows the bytes), (2) "Raven Feathers consumed" stat stuck at 4 — `body+0x25` writes don't take, mirror-cascade pattern, (3) chapter-achievement records source unmapped — drives a compounding-blanks bug on the score-details page where leftover invalid achievement slots interleave between chapter-1 and chapter-2 active icons.
  - **Ghidra deep dive — ingredient subsystem**: identified + named `oSDtHeroIngredient_Serialize` (vtable[3] @ 0x14037c100), `hero_ingredient_add_or_remove` (FUN_14038c900 — adds/removes by `param_2` int type-id), `find_hero_ingredient_definition_by_id` (matches `*(int *)(*plVar1 + 0x290)` against caller's int), `oCDtIngredientDefinition_ctor`/`_typedesc_init`/`_Serialize` (vtable[3] @ 0x140318cb0), the hash-derivation chain `FUN_1404e73f0` → `FUN_140506950(0, CRC32(string))` that populates the IngredientDefinition's +0x290 type_id from a string at load time. Multiple `serde_vec_*` generic vector serializers named for clarity. Defined `oSDtHeroIngredient` C++ struct in Ghidra. ~20 function/data renames + 20 variable renames + 1 plate comment all persisted in the Ghidra DB. Ingredient wire format = u32 type_id + u16 count (8 bytes per record) — but the type_id values for "Key" / "Bean" / etc. could not be calibrated without a non-empty ingredient vector to anchor on, so add-ingredient editing is blocked until we get a save with non-empty inventory.
  - **Documentation + recipes**: added "Edit recipes" section to `rw/findings/save-edit-pipeline-2026-04-30.md` — copy/paste Python scripts for (a) Stars of Fate edit, (b) full mint chain (zero every per-run stat we know how to), (c) diff two saves, (d) splice/insert into a body; documents the gaps in the mint recipe explicitly. Added new CLAUDE.md rules: "Saves are only generated at chapter-boss kills" (with the ~20-min real-time-play cost ceiling), "Ghidra: annotate findings on the spot" (rename functions / data / params / locals / structs / equates immediately, narrative WHY belongs in key-findings not Ghidra comments). Also accepted parallel-agent's CLI design rules + cipher-position confirmations.
  - **Triage cleanup pass**: created new `rw/findings/save-mint-status.md` consolidating the three open mint-recipe questions with hypothesis lists + discriminating tests; cross-references `live-state-mirror-cascade.md`. Trimmed conquered-content out of `rw/findings/geppetto-save-analysis.md` (CRC32, XP, Level, Chapter counters, Stars of Fate all moved out — now in key-findings) and `rw/findings/level-runtime-address.md` (vtable + class layout already in `oe-dynamic-listener-data.md`). Archived `rw/findings/talent-record-discovery.md` to `rw/findings/` (was CLOSED).

EXPERIMENTAL-2: Save-edit pipeline + decoder/encoder + nested-body editing (2026-04-30)
  - built `tools/rerw-src/lib/cooked.py`: bidirectional decoder/encoder for the oEngine "Cooked" binary serialization format (used by every `.gen` file in `_Cooking/` and, with a header variant, by `Profile_1.ob` save files); round-trips byte-for-byte across all test corpora (Modal_Save_Or_Quit 2.8 KB, Geppetto herodef 18.9 KB, Hero_Geppetto entity 620 KB, Profile_1.ob saves)
  - identified file structure: 16-byte header (variant: `.gen` carries `"Cooked"` magic, `.ob` carries 4-byte CRC) → `0xAABB1111` class registry → `0xAABB2222` → object section. Per-class metadata is 16 bytes after name: `m_uId` u32, `m_uVersionMaj` u16, `m_uVersionMin` u16, `schema_version` u32, `m_uParentId` u32. `schema_version` (initially mislabeled `field4`) is the version gate Serialize methods compare against
  - confirmed save-file CRC is standard zlib CRC32 over body bytes from offset 0x10 to EOF; verified empirically (`zlib.crc32(data[0x10:]) == stored_hash`) and identified loader's verification path (`save_load_parse_top_level` image+0x64aa50 returning error 4, `crc32_zlib` image+0x505cc0). Encoder auto-recomputes on edit
  - discovered two-tier object structure: top-level instances listed in the leading instance index table + nested sub-objects embedded inside parents' bodies via inline `0xAABB1111`/`0xAABB2222` markers. The wrapper `oCDtGameProfile` lives AFTER the 1199 top-level frames. Most user-visible runtime classes (`oCDtPlayerProfileData`, `oCDtCurrentRunProfileData`, `ActivityScore`, `HeroScoreData`, `HeroMOPersistentData`, all 649+ event listeners) are nested, not top-level
  - reverse-engineered Serialize methods for `oCDtPlayerProfileData`, `oCDtCurrentRunProfileData`, `ActivityScore`, plus the two helper sub-serializers `FUN_1401c5e30` (read 2 strings) and `FUN_140670af0` (read u32 + 2 strings + u32 + optional string). Documented per-field offsets and version gates
  - built recursive `parse_object_tree` walker exposing the nested structure as a `TreeNode` tree with `find_class_in_tree` for class-aware traversal; added `--tree` and `--tree-class` CLI flags
  - confirmed empirically that cross-save record transplant is impractical: object references are u32 indices (`uOjbectId`) into the per-load object pool; `oCBinaryLoader_ReadObjectRef` (image+0x4e7f50) reads them with sentinels `0xFFFFFFFF`=null / `0xFFFFFFFE`=self. Clean and chapter2 saves have different orderings AND different class registries (clean=26 classes, ch2=39), so neither save's bodies hold valid indices in a reassembled file. Naive append-at-end, full-class-replace, and index-aligned strategies all fail. Genuine cross-save migration would require per-class schema knowledge to remap every embedded index — deferred
  - proved nested-body editing works as the actual mechanism for path B (scenario crafting via save edit). Two patterns: (a) replace a nested body with a synthesized minimum that exactly matches the wire format the parser will read (proven on `ActivityScore` with a 25-byte minimum body), (b) zero specific scalar/float fields in place while preserving structural counts and length-prefixed strings (proven on `HeroScoreData` and `HeroController`)
  - mapped the actual storage of every user-visible end-of-run stat by edit-and-test: damage dealt + damage taken + per-source breakdowns live in `oCDtEntityCpntHeroControllerPersistentData` at byte-misaligned body offsets `+0x11`, `+0x15`, `+0x19`, `+0x1d`, `+0x35d`; playtime float lives in `oCDtCurrentRunProfileData` own body at `+0xe5` (also byte-misaligned); `HeroScoreData` and `ActivityScore` are cached snapshots that don't drive the display but are good practice to zero for consistency. Verified end-to-end by build → install → load → abandon → observe; runtime correctly accumulates from zero on next play
  - integrated with existing `rerw write savefile` GUID-locator pipeline (`tools/rerw-src/data/save-fields.yaml`); produced combined edits (zeroed stats + level set to 1 via `--level 1`) that load cleanly with HUD reflecting the new hero level
  - documented the modal-error-4 false-negative gotcha in `CLAUDE.md`: clicking OK on the error modal can route to the Continue/New-Game dialog (load succeeded despite the modal warning) OR to fresh-account hero selection (true failure, quit before next save event clobbers local). All previous test reports stating only "got the error modal" were ambiguous; we re-tested several to discover several "failures" were actually false negatives, and at least one (`transplant_indexed`) was a true failure that wiped local unlocks
  - identified the cumulative "Level reached" stat as a separate, lifetime-accumulating field NOT in any record we touched — likely lives in `oCDtHeroProfileData` per-hero data; deferred
  - new Ghidra renames (persisted): `oCDtPlayerProfileData_Serialize` (0x1401da580), `oCDtCurrentRunProfileData_Serialize` (0x1401da9a0), `ActivityScore_Serialize` (0x1401da440), `oCBinaryLoader_ReadObjectRef` (0x1404e7f50), `save_load_parse_top_level` (0x14064aa50), `save_load_parse_object_section` (0x1404e8690), `save_load_read_header_flags` (0x1404e7c50), `crc32_zlib` (0x140505cc0), plus typedesc init functions and vtable / typedesc data labels for the three reversed classes
  - new key-findings docs: `rw/findings/decoder-work-2026-04-30.md` (decoder build plan), `rw/findings/save-edit-pipeline-2026-04-30.md` (canonical session findings + `lib.cooked` tool reference + verified stat-source mapping + critical gotchas)
  - cipher.py fix: confirmed and applied position 40 (uppercase `O`) as identity (was `#` unknown), via empirical filename match on `Hrtgl_Fgkq_Ou_Pwdi.qzidis.ri` ↔ `Modal_Save_Or_Quit.entity.ot`; new path-aware helpers `encipher_path` / `decipher_path` and `transform_cooked_path` added to handle full filesystem paths
  - new harvested files for format work: `rw/dumps/modal_save_or_quit/Modal_Save_Or_Quit.entity.ot.EntitySettingsResource.gen` (decoded), `rw/dumps/geppetto/Geppetto.herodef.ot.DtHeroDefinition.gen`; lab edits exercising the pipeline: `rw/saves/edits/lab/{transplant_test,transplant_test_player,transplant_selective,transplant_full,transplant_indexed,edit_test_player,min_activity_scores,zero_activity_scores,zero_scores,zero_scores_v2,zero_scores_v2_level1,remove_activity_scores}/Profile_1.ob`

EXPERIMENTAL-1: Frida save-trigger pipeline + save subsystem RE (2026-04-29)
  - mapped the save subsystem end-to-end via static analysis: `save_request_sync` (image+0x6797b0), `save_request_async` (+0x679760), `save_atomic_orchestrator` (+0x678f30) on the worker thread, single ring queue at `DAT_14143ffc0` semaphore-signaled, atomic write via Temp+CopyFileW (no rename); function map and field offsets captured in the new key-finding
  - identified `oCDtRootGs` as the run-state class, with the embedded IO job at `instance+0x1928`; vtable[0] (= image+0x1c6830) is the unique discriminator used to find heap-allocated instances at runtime
  - built Frida pipeline (`tools/frida/save_now.js`) that locates the live `oCDtRootGs` via vtable-signature heap scan and calls `save_request_sync(NULL, instance+0x1928)` from outside the game; pipeline mechanically works (Profile_1.ob mtime updated, CRC valid, in-game "Now Saving" overlay confirms the trigger goes through the real save path) but produced output is content-incomplete — engine refuses the file as resumable on relaunch
  - root cause: `save_request_sync` does not introspect run state — it queues a write of bytes already at `job+0x30`/`+0x38`. A separate prep/serializer step (location TBD) populates that buffer in the natural save chain. Three-save model surfaced via parallel static work: GameSessionGs root job + profile-manager async at `profile+0x50` + global async at `DAT_14140dd70+8`. Continue-gate may depend on profile-manager / global metadata in addition to the run payload
  - identified the save dialog hook: `FUN_14027fde0` loads `DAT_141410b38` (`Modal_Save_Or_Quit.entity.ot`), stores it at `session+0xf8`, wires a callback pair, and sets UI/game flags — the upstream entry point for the natural save chain
  - debunked the "saves are account-locked" hypothesis: account ID does not appear in any save in any encoding; the cross-account lock is Steam Cloud sync, not in-game logic; documented in `rw/findings/save-account-binding.md`
  - new key findings: `rw/findings/save-subsystem.md` (architecture, function map, queue ABI, job struct fields), `rw/findings/save-account-binding.md` (Steam Cloud as the lock)
  - new tooling: `tools/frida/save_now.js` (save trigger), `tools/frida/diagnose_save.js` (owner-discovery + job-buffer inspection without recursive ref scans), `tools/frida/repl.js` (interactive REPL toolkit), `tools/frida/README.md` (WSL→Windows interop, Frida 17.9.3 API gotchas), `tools/frida/HANDOFF.md` (active-investigation operational doc)
  - new setup doc: `rw/docs/ghidra-windbg-mcp-for-wsl.md`; new top-level config `.mcp.json` registers Ghidra (127.0.0.1:8080) and WinDbg (127.0.0.1:8000) MCP servers
  - new triage docs: `rw/findings/ghidra-rule-c-investigation.md`, `rw/findings/items-add-primitive-cap.md` (engine-validation ceiling for item slot 5)
  - new artifacts: `rw/saves/proofs/geppetto/chapter1/frida-trigger-1/` (Frida-triggered Profile_1.ob + PROVENANCE with byte-level analysis vs chapter-2 known-good), `rw/saves/edits/golden/geppetto/chapter2/laser-lenses_1/item-add-fill-moonstone-stack-5of5/` (item-add primitive cap test golden)
  - updates: `rw/findings/magical-objects.md` (run-state body layout, items count at body+0x59, trailing block structure), `rw/docs/README.md` (linked tools/frida/HANDOFF.md as active investigation marker)
  - next: WinDbg session with hardware watchpoints on the three job buffer pointers + BPs on `FUN_14027fde0` and the chapter-end orchestrators to capture call stacks during a natural Save & Quit click; goal is identifying the prep function(s) and the dialog-trigger chain so the natural save can be invoked without finishing a chapter run

MAINT-12: items-record decode + hero/item YAML schema migration (2026-04-29)
  - decoded the magical-object item record format: tag=0x1a records nested inside the tag=0x12 run-state record, each 32 bytes (marker + tag + 16-byte runtime GUID + u32 sequence counter + close); cataloged 114 entries (68 magical objects + 46 powerups) via `oCDtMagicalObjectProfileData` tag=0x05 records
  - lab-verified items mechanism end-to-end: chapter-2 Save A Vorpal Blade swap (`Kill_Low_Life_Enemies` runtime GUID `cf7d88d6…` → `Avoid_Death_Once_Per_Chapter` runtime GUID `1cc781a5…`); in-game display changed (Vorpal Blade → Water of Life) and the on-revive label flashed during follow-up lethal-damage test, confirming the swapped runtime GUID drives both display and effect resolution
  - identified the Mortar migration ghost: pre-rework Baba Yaga's Mortar entity (`Avoid_Death_Once_Per_Chapter`, Legendary folder) is rerouted via inheritance to `Full_Heal_At_Day_Night.entity.ot` (Water of Life), with the legacy revive effect retained alongside Water of Life's vitality bonus — sole confirmed hybrid in the catalog; new-Mortar effect lives in Cursed/`Destroy_Legendary_To_Damage`
  - runtime-GUID extraction recipe for any item entity file: 16 bytes immediately preceding the `[u32 strlen=22]"Dt Magical Object Data"` anchor; verified against 114-of-114 entity files (5 cross-checked against active save records)
  - new key-finding: `rw/findings/item-table.md` (canonical 114-item catalog with effect filename, icon, name-key, catalog GUID, runtime GUID, rarity per row; ghost migration history; verified aliases section)
  - new key-finding (in-flight): `rw/findings/save-catalog-flag-bytes.md` (7 distinct 12-byte flag patterns across 114 catalog records; hypothesized MO/Powerup distinction via byte-3 = `0x03` vs `0xff`; per-row attribute interpretation pending)
  - new registry: `tools/rerw-src/data/magical-items.yaml` (68 entries; 67 clean + 1 Mortar ghost block with `displays_as` / `inherits_from` / `meta_notes`; 9 legacy upgrade-pair bases share canonical's `display_name` + `key`; 59 entries fully aliased from Ravenswatch wiki tables (Common/Rare/Epic/Legendary/Cursed))
  - new registry: `tools/rerw-src/data/powerup-items.yaml` (46 entries; `key:` field present, alias values pending helper curation)
  - migrated all 12 hero YAMLs to new schema: `display_name` (player-facing) + `key` (PascalCase, no spaces/punct) + `controller_name` (engine `Skill Controller XXX`) + `guid` + at-most-one marker (`is_start: true` for the 4 starting talents per hero | `is_ult: true` for the 2 base ults | `ult_upgrade_for: "<UltKey>"` for the 4 upgrades) + `desc: |` block (verbatim wiki effect text + `Changes per rarity:` line + rarity changes)
  - dropped the `hero: <Name>` top-level field from hero YAMLs (filename is sufficient)
  - documented the `ult_upgrade_for` derivation rule: SUFFIX number M in `Skill Controller Ultimate N Upgrade M` determines the parent ult (Upgrade M → `Ultimate Power M`'s key), NOT the prefix N — fixes an incorrect presumption in `talent-records.md` that paired by the prefix
  - all 12 hero files populated from Ravenswatch wiki tables (28 talents per hero × 12 heroes = 336 entries); display_name + desc + rarity_changes verbatim from wiki
  - rerw CLI integration deferred: registry YAMLs ready for `rerw read savefile --items` and `rerw write savefile --item-slot N --item-id <id>` but not yet wired in `commands/{read,write}_savefile.py`

PRP-2: rerw talent + tier edit primitives (2026-04-28)
  - decoded the talent record (tag=0x12) and tier byte (tag=0x10) for Geppetto saves: 5×16-byte talent GUIDs at the talent-pick block (anchored by `[u32=0][u32=5]` sentinel), plus a u8 tier byte at offset GUID+17 of each first-occurrence tag=0x10 record
  - verified end-to-end with 3 lab swaps on the chapter-2 Geppetto proof — talent swap (slot 1: Special Creates Dummy → Trait Twins, displayed as Common), tier edit (Dummy Ball Common → Legendary), combined edit (slot 1 → Trait Twins at Legendary); all three landed as goldens at `rw/saves/edits/golden/geppetto/chapter2/laser-lenses_1/talent-slot1-*` with `info.md` per mod
  - tier-value mapping: `0=Common, 1=Rare, 2=Epic, 3=Legendary, 4=ult-marker` (slot 5 ult uses ult-marker, no real tier)
  - extended the YAML field registry with a new `talent_picks` field type (record_guid + sentinel + slot_count + skills_data_dir); `lib/save_fields.Field` now carries an `extra` dict for type-specific metadata
  - new `lib/talent_edit.py` with `find_talent_record`, `find_picks_anchor`, `read_picks`, `write_pick`, `find_tag10_record`, `read_tier`, `write_tier`, `parse_tier`, and `detect_hero` (parses `Heroes\<Name>.herodef.ot` from the save body)
  - new `lib/skill_controllers.py` loader for per-hero YAMLs with case/punctuation/prefix-insensitive resolver (`_normalize` strips "Skill Controller " prefix and collapses whitespace/dashes/underscores), defensive alias cleanup at load time (skips empty/null/duplicate/redundant-with-canonical entries)
  - `rerw write savefile`: new `--talent-slot N`, `--talent-id <name|alias|guid>`, `--tier <name|0..3>` flags; combined edits run atomically (one CRC pass); slot 5 rejects `--tier`; talent + tier edits route to the *current* slot occupant's tag=0x10 record (so swapping a slot's talent + tier in one invocation works correctly)
  - `rerw read savefile`: new `--talents` flag printing slot picks with hero auto-detected, alias-preferred display, tier name + hex byte
  - harvested all 12 hero herodef binaries from the cooked game install (`Heroes/<Name>.herodef.ot.DtHeroDefinition.gen` after substitution-cipher decode), generated `tools/rerw-src/data/heroes/<hero>.yaml` for each — 28 skill-controller GUIDs per hero (336 total); Geppetto YAML has 7 verified player-facing aliases from gameplay (Twin Dummies, Family Meeting, Clockwork Medicine, Sharp Noses, Overclock, Dummy Ball, Laser Lenses), other heroes ship with empty alias lists pending external curation
  - new key finding `rw/findings/talent-records.md` (full byte layout, GUID encoding, tier mapping, the L1–L10 leveling table, all 28 Geppetto controllers); updated `save-binary-format.md` to point to it; consolidated talent-name reference at `rw/findings/talents.md`
  - investigation history at `rw/findings/talent-record-discovery.md` (covers ruled-out hypotheses including the body+0x35 u32 false lead and the base64 run-id blob)
  - new chapter-2 proof save `rw/saves/proofs/geppetto/chapter2/twin-dummies-all-legendary-talents/Profile_1.ob` (Twin Dummies / Family Meeting / Clockwork Medicine / Sharp Noses / Overclock, all 4 tiered talents at Legendary)
  - verified scope: slots 1–5 only on Geppetto saves; slots 6–10 (L6–L9 regular picks + L10 ult upgrade) and cross-hero generality are unverified — flagged as follow-ups in the key-finding doc

MAINT-8: Hero-swap edit primitive + save-format consolidation (2026-04-27)
  - verified hero record format: length-prefixed ASCII path `Heroes\<Name>.herodef.ot`; locate via `data.find(b'Heroes\\<name>')`, length prefix is `u32 LE` immediately preceding
  - verified hero swap end-to-end across 4 swaps from chapter2 proof: Carmilla (8-char, no shift), Aladdin (7-char, −1 byte), Snow_Queen (10-char, +2 bytes), Red (3-char, −5 bytes); all loaded with full identity + run-state preserved; engine tolerates body shifts across `−5..+2` byte range
  - observed: slot 1 auto-populates with new hero's L5 ultimate on swap; slots 2/3/4/5 cleared; resolver picks per-hero ult index (3× ult #1, 1× ult #2)
  - level-downgrade test (chapter2 proof, L5 → L1): engine accepts inconsistent state (low-level char + high-level talents); per-ability damage couples to level field (Geppetto hammer strike 23 @ L1, 34 @ L5, same talents); XP value preserved, threshold tracks level
  - disproved `ProfileDreamShards` identification (`b43eeb58…`): writes don't affect displayed profile shards (verified 9999 → in-game still showed 21); pulled from verified-fields table; relabeled `_unknown_b43eeb58` in `mod_save.py`
  - empirically confirmed Steam Cloud sync constraint: cloud restores on game launch following any session that loaded a save; each in-game test is single-session
  - new key findings: `rw/findings/save-binary-format.md` (canonical), `rw/findings/hero-swaps.md`, `rw/findings/hero-table.md`; archived `oe-dynamic-listener-data.md`, `save-chapter-counter.md`, `oe-listener-mining.md` under `key-findings/archive/` and `docs/archive/` with superseded-banner headers
  - new goldens: 4 hero-swap variants + `chapter-rewind-from-ch3-level99` under `rw/saves/edits/golden/geppetto/`
  - updates: `CLAUDE.md` swap-op rule + cloud-sync guidance, `rw/docs/playbook.md` lab directory convention (`<hero>/<run-name>/<mod-id>/`) + failed-test deletion rule + Ravenswatch wiki reference, generated `rw/ref/tree-deciphered.txt` from ciphered tree

PRP-1_save-chapter-edit-primitive (2026-04-27) — extended `rerw` with `read savefile` and `write savefile` commands backed by a YAML field registry; rerw 0.1.0 → 0.2.0
  - `read savefile --source FILE [--chapter] [--level] [-v]`: print field values; no field flags prints all, flags filter to specific fields; reports `<not present>` when a GUID isn't in the save
  - `write savefile --source FILE --dest DIR [--chapter N] [--level N] [-f] [-v]`: edit fields atomically (one read, all writes, single CRC32 recompute, one output file); `--force` / `-f` bypasses overwrite prompt
  - New top-level `read` and `write` Click groups in `cli.py` mirroring the existing `swap_` group; `savefile` registered under each via `cli.add_command()`; REPL builtins deferred (TODO marker)
  - Default output prints `Source savefile: <path>` header on both commands; write also shows `<field>: <old> -> <new>` and `CRC32: <old> -> <new>` plus `Wrote <path>`; `--verbose` / `-v` adds load summary and per-GUID locate+write detail
  - New YAML registry at `tools/rerw-src/data/save-fields.yaml` top-level grouped by shape (`scalar:` populated with chapter and level; `array:` / `ref:` reserved for future inventory / item-id work); each field has `type`, `guids` list, `description`
  - `lib/save_fields.py` (loader, `Field` dataclass, `SaveFieldsError`); `lib/save_edit.py` (CRC + GUID-locate + `read_field` / `write_field`, no Click)
  - Added `pyyaml >= 6, < 7`; workspace lock sync via `just tool-sync-all` (rerw-src for pyyaml resolution, rs-src for stale 0.1.0 → 0.2.0 lock from MAINT-6)

MAINT-7: Save chapter-counter edit primitive (2026-04-27)
  - verified the in-run chapter is stored as two parallel int32 LE records keyed by GUIDs `13fa8e2c...` (opaque) and `6661756c74...` (All_Chapters GameMode reference; ASCII tail `faultdef.ot&`)
  - both progress in lockstep across the chapter2/chapter3/epilogue Geppetto proofs
  - encoding is "chapters completed" — chapter1=0, chapter2=1, chapter3=2, epilogue=3, with the records absent entirely in clean saves
  - chapter3 save edited to chapter=0 (both GUIDs in lockstep, CRC32 recomputed) loaded into chapter 1 with full chapter-3 run-state intact (level, gear, currency, items preserved)
  - produced verified golden at `rw/saves/edits/golden/geppetto/chapter1/laser_lenses_1/chapter-rewind-from-ch3/Profile_1.ob`
  - wrote `rw/findings/save-chapter-counter.md` (GUIDs, value semantics, edit procedure: find GUID + 15, write int32 LE, recompute CRC32 at offset 0x0C, install via `rerw swap savefile`)
  - deciphered GUID B's asset path as `All_Chapters.gamemodedefaultdef.ot.GameModeDefaultDefinition.gen` via `rerw decipher`
  - trailing 4 bytes `26 ba 45 19` are plausibly part of the same hash family used for other save-record GUIDs (Level, ProfileDreamShards) — reversing it is the proposed path to programmatic item editing
  - edit performed via existing `rw/scripts/mod_save.py`
  - promoting editor surface to `rerw save` (or alternative shape under discussion) is planned follow-up

MAINT-6: rs read/write/find/watch primitives + shim verbose logging (2026-04-26)
  - added `rs read <addr>`, `rs write <addr> <value>`, `rs find <hex>`, and `rs watch <addr>` Click commands at `tools/rs-src/commands/`, scaffolded via `scafcli tool add command` so the auto-wiring markers in `cli.py` stayed authoritative
  - all four commands follow the existing convention (default single-line output, `--verbose`/`-v` for the `tree-lib` tree)
  - `read`/`write`/`watch` accept a typed `--as` (hex|int32|uint32|int64|uint64|float32|float64|bool) with addresses parsed from `0x...` hex or decimal
  - `find` defaults to a summary (count + first 20 inline) with `--all` and `--out FILE` escape hatches
  - `watch` opens one TCP socket via the new `lib/shim_client.Session` context manager and multiplexes polls over it
  - introduced `lib/value_codec.py` as the single source of truth for the type table (sizes, struct formats, encode/decode, address parsing)
  - kept `lib/shim_client.call` semantics unchanged so existing one-shot commands and tests are untouched, but extracted a `_parse_response` helper shared between `call` and `Session`
  - updated `rw/scripts/windows/rs_shim.py` to log each RPC to stdout (method + param summary + result summary; long hex payloads truncated; addresses rendered as hex), default on with a `--quiet` flag to suppress
  - refreshed `rw/docs/tools/rs.md` with shim stdout-logging behaviour and the persistent-connection model
  - bumped `tools/rs-src/CHANGELOG.md` to 0.2.0
  - tests cover the new commands' quiet/verbose paths, error translation, encode/decode round-trips for every supported type, persistent-session reuse and exit semantics, and the find summary/truncation/file-dump output modes

MAINT-5: rs trainer scaffold and Windows-side shim (2026-04-26)
  - introduced `rs` Click CLI at `tools/rs-src/` with `attach`/`detach`/`status`/`dev sync-shim` commands and prompt-syncing REPL via `repl-lib`
  - auto-detach on `exit` when attached
  - verbose attach mode renders four-step tree via `tree-lib`
  - new `lib/{config,errors,paths,shim_client}.py` with typed `ShimError` hierarchy, env-var override chain (CLI flag > env > default), WSL2 gateway auto-detect, and WSL→Windows path display conversion
  - introduced `rw/scripts/windows/rs_shim.py` (pymem RPC server: ping/attach/detach/modules/regions/read/write/find over line-delimited JSON/TCP, idle until rs calls `attach`)
  - env vars `RS_SHIM_HOST` / `RS_SHIM_PORT` / `RS_SHIM_LOC` documented in the bash wrapper
  - wrote `rw/docs/tools/rs.md` (architecture, deps for Ubuntu/Debian + Void, smoke-test recipe)
  - added session1 HUD log at `rw/saves/edits/golden/geppetto/chapter2/laser-lenses_1/session1_hud_log.md` preserving raw per-snap stats
  - tests cover command flows, RPC error translation, config resolution, REPL dispatch + exit hook, and path conversion

MAINT-4: OEngine listener-data RE; pin Geppetto runtime stats (2026-04-25)
  - identified `oe::DynamicCpntValueListenerData<T>` as the engine's observable-value pattern via intact MSVC RTTI walk (vtable → COL → TypeDescriptor → mangled name)
  - mapped all 10 `<T>` template specializations and their vtable RVAs in `Ravenswatch.exe`
  - pinned Geppetto runtime stats (Level, XP threshold, XP current, dream shards, HP current/max, crit chance) from a within-process L1..L5 capture, with chapter-2 transition verification narrowing candidates
  - confirmed stars-of-fate as a plain int32 field (not listener-wrapped) and damage as computed-on-demand (no stable storage)
  - added `mem_snapshot.py` subcommands `read`, `find-progression`, `modules` plus `bytes_at_sorted` helper
  - produced `rw/docs/oe-listener-mining.md` techniques playbook, `rw/findings/oe-dynamic-listener-data.md` (RVA table + per-session workflow), `rw/findings/level-runtime-address.md`, and session1_* artifacts under `rw/saves/edits/golden/geppetto/chapter2/laser-lenses_1/`
  - cross-process intersect artifacts moved to `old_data/`

MAINT-3: Memory diff workflow, rerw save swap, REPL (2026-04-25)
  - added `rerw swap savefile` command (with `--source`/`--dest` flags, `RERW_SAVEGAME_DIR` env var, default WSL path to Ravenswatch's `_Save`) and `rerw interactive` REPL backed by `repl-lib`
  - REPL has a custom `swap-savefile` built-in (bivalent: bare enters sub-mode, with flags runs inline) and a context-aware `exit` (leaves sub-mode if in one, else exits REPL)
  - reworked `rw/scripts/windows/mem_snapshot.py` from A/B-pairs into a baseline+diffs+intersect workflow with sequential or labeled grabs (`grab` = next `grabNNN`, `grab <label>` = custom name), strict no-overwrite, `.baseline` marker file, `--out-dir` flag, expanded capture filter (PRIVATE/IMAGE/MAPPED), and live progress counter
  - wrote `rw/docs/tools/mem-snapshot.md`
  - produced golden modded saves at `rw/saves/edits/golden/geppetto/chapter2/laser-lenses_1/{level5,level8,level14,level17}/Profile_1.ob` with `info.md` describing the level-detect test
  - reorganized Python scripts (`scripts/python/rw/{analyze_save,mod_save}.py` → `rw/scripts/`, removed `caesar_crack.py`)
  - relaxed `scripts/run-tests.sh` CLI-name regex from `^[a-z]+cli$` to `^[a-z]+$`
  - added rerw tests (REPL dispatch and built-ins, swap_savefile env/flag/fallback precedence, lib/paths)
  - ignored REPL history files

MAINT-2: Scaffold Ravenswatch RE workspace and rerw tool (2026-04-25)
  - relaxed scafcli naming to make `cli` suffix optional
  - renamed `hexsircli` → `hexsir`
  - created `rerw` tool with `cipher`, `decipher`, and `harvest` (group stub) plus shared `lib/cipher.py`
  - established `rw/` workspace (`ref/`, `harvested/`, `dumps/`, `triage/`, `key-findings/`, `saves/{proofs,edits/{lab,golden}}`) with `.gitignore` rules
  - wrote `rw/docs/{README,playbook,tools/}` documenting structure, promotion paths, workflow, and doc conventions
  - added `rw-findings-report` skill
  - migrated `interim/` content into `rw/`
  - converted `ANALYSIS_SUMMARY.txt` into `rw/findings/geppetto-save-analysis.md`
  - marked `.ai/docs/rw/` deprecated
  - registered `rerw` in `prime-full-tooling.md` and `scripts/run-tests.sh`

MAINT-1: Initial hexsircli and repo branding (2026-04-23)
  - added `tools/hexsircli` and `tools/hexsircli-src/` with commands (basic, header, scan, verify)
  - registered in `scripts/run-tests.sh`
  - updated README.md with Hexsir branding
  - configured `.claude/commands/prime-full-tooling.md` and `prime-quick-tooling.md` (Tool Suite: hexsircli, scafcli; Indexed-only: scripts/run-tests.sh)
  - updated `.gitignore` with Python build artifact patterns
  - removed project-specific scafcli tests
<!-- DONE_END -->
