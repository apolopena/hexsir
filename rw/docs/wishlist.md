[← Back to docs](README.md)

# Wishlist

Future-work ideas that come up mid-session and shouldn't drift into the void. Not commitments — just things to consider when the time is right. Add freely, no codes or headers required. When an item is picked up, move it to `.ai/TASKS.md` (with a task code) and delete it here.

---

## CLI surface

- **Per-stat individual setters.** `save_mint.setters` has primitives for editing playtime, score floats, damage-dealt, chapter-progression banner, ActivityScore records, dream-shards-earned, feathers-consumed, dream-shards-spent, etc. None are exposed as standalone `rerw write savefile <field>` commands — they only fire wholesale via `rerw mint savefile`. Open up surgical control for any of those if a use case arises.
- **`rerw mint savefile` flavor flags.** Right now mint zeroes everything. A flag like `--keep-playtime` or `--keep-scores` could selectively preserve fields. Useful if we ever want a partial mint.
- **Mint chain shortcut.** `rerw mint savefile --hero romeo --level 14 --rarity legendary --clear-picks` would collapse the current 6-step chain into one call. Worth it if Romeo-style mints become a frequent pattern.

## Investigations / digs

- **Continue-vs-new-game gate.** What marker the engine reads at title-screen time to decide whether to show "Continue" on the main menu. Blocks the clean-save splice path — without it `add-level-record` and any "build a save from scratch" workflow can't produce loadable saves. Tracked in `rw/findings/frida-pipeline-hardware-breakpoint.md` "Parallel investigation."
- **Master chapter seed location.** Test plan in `.ai/scratch/master-seed-test-steps.md`. Capture the displayed seed format, Frida-save, byte-search the dump for LE/BE/ASCII encodings, document offset.
- **Item picker / chest / Sandman shop RNG harness.** Clone `tools/frida/force_seed.js` for non-talent picker entry functions. Mechanical once each entry RVA is known. Per `rw/findings/random-seed-system.md`.
- **Save prep/serializer function (Frida).** Hook `oCMemoryBinaryStream::Write` (image+0x5257d0) with a counter, log `Thread.backtrace()` on call #1 only — captures the chapter-end serializer entry that Ghidra's static xrefs can't see. Per `rw/findings/frida-pipeline-hardware-breakpoint.md`.
- **Host-mutates-peer in-run state (MP).** With shards proven (host writes `HC+0x1590` raw, peer's per-frame `HC_per_frame_update` watcher detects + dispatches, replication propagates), generalize the pattern: which other peer-character state lives in host-authoritative replicated stat blocks the host can write? Concrete target: in-run effects of Stars of Fate (the buffs / extra slots / damage modifiers applied at chapter init from the peer's profile). If those effects land in a replicated structure host has write access to, host can grant "buffs as if SoF were unlocked" to all peers in-run. Untested. Investigation surface: trace where peer's chapter-init profile-load lands SoF-derived modifiers in HC's struct, identify whether they're in replicated regions, attempt host-side write + observe peer-side effect. (Persistence is out of scope — MP doesn't fire save events; in-run only.) Per `rw/findings/multiplayer-host-authority.md`.
- **GUID hash-tail reversal.** The 4-byte tails of save-record keys (Level: `5793e600`, etc.) are likely a hash. Crack the hash function so we can mint arbitrary save-record keys for asset paths. Per `rw/findings/save-guid-hash-tail.md`.
- **Live source-of-truth pin identity.** All known stat pins are mirrors, not authoritative. Trace `IDynamicValueListenerData` callback chain backwards from any listener instance. Per `rw/findings/pin-identity-uncertain.md`.
- **Remaining held resources.** Wood, bean, and other ingredient-type held resources don't have located fields. Walk HC/CRP serde to find them. Per `rw/findings/save-mint-status.md` item 2.
- **Per-hero verification of the talent record format.** Currently verified only for Geppetto. Run the lab-build / verification cycle on a non-Geppetto save to confirm the structure generalizes (talent-records.md notes this is presumed but unverified).
- **Side-boss force-spawn + stats-tracking parity check.** `forceBossSpawn` (chapter-boss arrival via `BossTimer[+0x12c] = boss_time`) does record the kill in the stats page (verified 2026-05-05). Open question: do key-boss (Den) or quest-boss (Sinbad / Roc / knights) kills get recorded if force-spawned via an analogous mechanism? Each of those has its own arrival/timer surface that hasn't been mapped — pre-req is finding the side-boss equivalent of `BossTimer_update` and the analogous "arrival" named-event hash. Out-of-scope for the chapter-boss-rush mod; relevant if the mod ever extends to side-boss-rush variants.
- ~~**Cross-chapter boss-rush mod (live, Frida).**~~ **Concluded structurally infeasible 2026-05-06.** Original goal: stay in chapter 1, fight Map Bosses 1→2→3→(4) back-to-back with no chapter transitions. **The engine loads exactly one chapter's content at a time** — entering a new chapter REPLACES the prior chapter's loaded assets; nothing is queued in the background. The path-based loader (`vtable[3]` of class type-id `0x53b64d` in `g_global_type_registry_root`) crashes when called cold from Frida — it requires engine-side level-load orchestrator state we can't replicate. Confirmed runtime: the encyclopedia in chapter 1 contains only chapter-1 spawner variants (`map_boss_spawner_dark_hills` and 5 siblings); `_storm_island`, `_avalon`, etc. are absent. To fight a different chapter's boss you have to actually load that chapter. The session 2026-05-06 work landed `tools/frida/mods/powers/ChapterBoss.js` (force boss arrival on the *current* chapter, instantly), which collapses the ~20-min chapter-end iteration to seconds — still useful, but it's "force this chapter's boss to arrive" not "rush all chapters' bosses." If a single-chapter variant (e.g. fight chapter-1 boss N times in a row with chapter-end suppressed) is wanted later, that's a separate, smaller-scope item; the chapter-end-suppression decompile traceback from `chapter_end_work @ 0x1402907e0` remains the open piece for that variant.
- ~~**Encyclopedia name-key dump tool (Frida).**~~ **Done 2026-05-05** — `dumpEncyclopedia()` in `tools/frida/mods/boss_rush.js`. Hash-based lookup doesn't work from outside (key is precomputed hash pair, not interned begin/end), so the implementation walks Swiss-Table entries directly. Per-entry name read via `entry+0x10 → value+0x08 char* / value+0x10 u32 length`. Confirmed working against the live `oCEntitySettingsEncyclopediaSceneContext`.
- **Universal entity-spawn API surface (Frida).** Beyond boss-rush: the engine has 40 separate `oCTLibrary<T>` catalogs (heroes, enemies, items, melodies, maps, tiles, rewards, dream shards, …) and a uniform `oIEntityCpnt` reflection surface (`vtable[11] getMemberByHash`, `vtable[19] getMethodHandleByHash`). The "developer console" surface — spawn any enemy, give any item, apply any melody — would be reachable from this combined with `fire_named_event` / `Sc_*`. **Status revised 2026-05-05**: the `oCEntitySettingsEncyclopediaSceneContext` we have working access to holds only 21 abstract entity-spawner classes (no enemies, no items, no melodies — those live in different catalogs). To reach each of the 40 `oCTLibrary<T>` catalogs, each one needs its own scene-context type-tester resolution and live runtime walk. Out-of-scope for boss-rush; not "integration only" — each catalog is its own dig.

## Frida tooling

- ~~**Decode `oCEntitySceneContext +0x520` array structure.**~~ **Done 2026-05-06 — disproved as the entity registry.** Decompiled the class registrar (`FUN_140186740`) and constructor (`FUN_1406e5a40`), and walked `+0x520` live: it is an asset/animation cache, not a live-entity registry (`Heroes\Carmilla\animation\carmilla_defensive_default_out.fbx1` found in entry[1]). The prior session's lead was wrong. Class fully mapped — registered size `0x4f8`, vtable RVA `0xf4fa10`, multi-inherit with embedded `oCEntitySpawner` at `+0xa0` and 6× `oCCallScheduleStep` sub-objects. Per-instance entity registry remains unfound. Full notes in `rw/findings/transporter-placement-primitive.md`.
- ~~**Find and teleport to active quest POI.**~~ **Partially done 2026-05-06.** Shipped `Transporter.warpPlayer(x, y, z, roundtrip?)` and `Transporter.warpEntity(name, x, y, z, roundtrip?)` (in `tools/frida/mods/powers/Transporter.js`). Player can warp to any landmark currently in the streaming asset cache via `RW.Entity.find(name).loc`. **Still open:** the encyclopedia is the streaming asset cache (15-30 entries near safe area; ~2000 deep into a chapter) keyed by asset path, so multiple physical instances of the same asset (e.g., 3-4 teleporters per chapter) share one entry — only one instance is reachable per name. Full per-instance enumeration requires finding the global "all live entities" registry (open dig). Active-quest POI specifically would also need `oCDtEntityCpntMinimapMarker` (type-id `0x15bc482e`) filtering once enumeration works.
- **Find the global per-instance entity registry.** Gates full POI enumeration, comprehensive boss-rush (find every camp/boss instance), dev-console (target arbitrary live entities). Promising paths: (1) extend `SpawnCapture` (now hooks `oCEntity` constructor at RVA `0x6c96f0` — fires once per entity creation across all subclasses) to capture across chapter-load AND combat windows; the captured entity addresses are the live instances. (2) Walk the scene_manager's full context list via the iterator at `scene_manager_find_context_by_type` (RVA `0x653f80`) — there may be a different scene-context type holding the live registry. (3) Hook the minimap render path — the UI knows about all POIs even when they're fog-of-war hidden.
- **`SpawnCapture` snapshot/merge semantics.** Currently `start()` clears prior captures, so re-running mid-test loses earlier data. Add a "snapshot then continue" or "merge into existing capture" mode for multi-window investigations.
- **Identify enemy class names from `initArg` templates.** Each captured enemy has an `initArg` pointer (the settings template — `oCEntitySettingsResource*` or similar). The settings has a name string at `+0x08` (per encyclopedia walker pattern). Read it during analyze() to print "12x Pig" / "4x Ghoul" instead of just hash addresses. Quick add to `SpawnCapture.analyze`.
- **Test camp-anchor spawn-children mechanic.** Move a non-hostile camp anchor near the player and observe whether the camp's spawn-children events fire near the new anchor position or at the original. If the spawn radius check uses the anchor's current position, this unlocks "summon enemies on demand." Untested. Pre-req: `Transporter.warpEntity` (shipped).
- **Locate the AI-target field on enemies; attempt redirect.** Seed observation: a captured Tentacle's `*(entity + 0x08)` was the Geppetto Dummy actor that was hitting it (consistent with current-AI-target). Conflict: a Summoner's `*(entity + 0x08)` was an allied Snake (not consistent with current-target — cultists don't target their own). First dig: walk a Tentacle's component hashmap to its `EnemyController` component and scan the component's bytes for the Dummy address, since the field may live on the component rather than the entity wrapper (parent backrefs are already known to live on the component, not the entity — same pattern). Static-side complement: search Ghidra for `Target`/`Aggro`/`Focus` symbol/string references and trace setters back to the field they write. If the field is found, attempt to write a different entity pointer into it and observe whether the enemy reorients — unlocks "make enemies fight each other" experiments. Per `rw/findings/enemy-ai.md`.
- **Classify visible-vs-anchor encyclopedia entries.** Cauldron, Boss Spawner, Teleporter confirmed visible (model attaches to the `+0x48` entity). NoModel, NoModel+2Cpnt, camps confirmed anchor-only (the visible thing is a separate child entity). Den Entrance and Entrance untested. Quick `Transporter.warpEntity` sweep with short roundtrips would build the table.
- **Native `HC_change_dream_shards` call for fast HUD animation.** `Currency.addShards` currently writes `HC+0x1590` directly. The HUD's "fast count-up" animation appears tied to the engine's natural event chain (downstream subscribers, modifier ticks like Hope Diamond / Heal-on-gain, MP replication). Direct write produces a slower visual count-up. Fix: call `HC_change_dream_shards` as a `NativeFunction` with a cached `oCCustomFlagList*` (captured from `args[2]` of a natural shard event). One natural earn-or-spend prerequisite event before the cache is primed; subsequent calls go through the engine's chain.
- **Telemetry sniff mod (Frida).** Companion to `tools/telemetry-warden/`. The warden blocks at the network layer but only sees TLS metadata (host, port) — request bodies are encrypted before WinHTTP. A Frida hook on `passtech_analytics_post_event` (RVA `0x1f6b10`) would read the JSON payload from process memory **before** encryption, giving full visibility into what the game tries to send (event_type, run_playtime, hero_name, hero_skin, difficulty, etc.). ~40 lines of JS, no certs, no system mods, no Python deps. Composes with the warden: warden blocks at the socket, sniff mod logs the body. Activate the mod alongside the warden for a session when you want sniff visibility; default solo play uses warden alone. Per `rw/findings/telemetry-surface-and-warden.md`.

## Reverse engineering / asset extraction

- **Deserialize cooked `.gen` local files.** We have the cipher-deciphered filename tree (`rw/ref/tree-deciphered.txt`) and harvested cooked binaries (`rw/harvested/EntitySettings/`), but **no static deserializer** for the engine's `oCEntitySettingsResource` `.gen` binary format. Building one would let us read entity / camp / spawner / map definitions offline (camp positions, enemy lists, quest layouts, etc.) without needing the game running. RE the engine's serialize/deserialize methods to understand the format. Today the runtime workaround is `dumpEncyclopedia()` against a live process, which only sees what's currently loaded; static dump would expose the full design-time data.

## Tooling / GUI

- **Tauri v2 desktop app — Build Tester (default) + dev superpowers (gated).** Front end Vue + Vite, build targets Windows and Linux (Windows first). Two modes selected at build time:
  - **Default build (no flag).** Ships only the "Build Tester" mode — a wizard-style GUI for creating specialized mints by driving `rerw` under the hood. Stub for now; final feature set defined when picked up.
  - **Build with `SUPERPOWERS=true`.** Adds a developer console: an xterm/conpty-powered terminal that wraps either `rerw` or Frida, with the ability to load external Frida scripts (e.g., `tools/frida/rw_lab.js`). Internal/dev only — do not ship publicly.

  The flag-gating must be a true compile-time decision (no superpowers code paths in the default-build binary). Decide on the IPC boundary between Vue front and Tauri back early — the Frida-script-loading path will want streaming stdout/stderr, which constrains the bridge.

## Documentation / housekeeping

- **`add-level-record` shelving decision.** Currently a half-primitive (structurally inserts a GroupLevel record but engine ignores it because the run-in-progress marker isn't set). Either delete the command entirely or move it to a "deferred" subgroup until the Continue-gate dig unblocks it.
- **Skill-controllers YAML schema mismatch.** `data/heroes/<hero>.yaml` uses `controller_name`/`display_name`/`key`/`guid`; the loader in `lib/skill_controllers.py` expects `name`/`guid`. Pre-existing bug (not introduced by this session). Causes `rerw read savefile` to print "controllers[0] requires string 'name' and 'guid'" instead of resolving talent names. Either update the loader or the YAML.
- **`rerw experimental` cleanup.** `add-level-record` and `find-talent-seeds` are the only two experimentals. Decide whether to keep `experimental` as a long-running shelf or fold its contents elsewhere.
- **Test-suite drift.** A handful of pre-existing test failures in `test_game_assets`, `test_lib_game_assets`, and one `test_lib_save_fields::test_missing_keys` regex mismatch. Unrelated to active work but should get cleaned up at some point.
